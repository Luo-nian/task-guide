# 任务指南 TaskGuide —— 运行指南

> 本文档告诉你明天起来怎么把项目跑起来。
> **重要前提**：AI 在沙箱里**无法编译 Android APK 和 Tauri 可执行文件**（缺 Android SDK / Rust 编译链），也无法实机测试。下面交付的是**完整可编译的源码工程**。能现场验证的部分（schema、Node 预览服务器、前端逻辑）已验证通过。

---

## 0. 项目状态总览

| 部分 | 状态 | 说明 |
|---|---|---|
| 需求文档 v1.2 | ✅ 定稿 | `deliverables/taskbar/2026-08-28/需求文档-任务指南.md` |
| 数据库 schema | ✅ 已用 sqlite3 实跑验证 | `task-guide/db/schema.sql` |
| 技术方案 / 同步协议 | ✅ 完成 | `task-guide/docs/` |
| 手机端 Kotlin 工程 | ✅ 源码完整（未编译） | `task-guide/mobile/` |
| 电脑端 Tauri 工程 | ✅ 源码完整（未编译） | `task-guide/desktop/` |
| 电脑端 Node 预览 | ✅ 已启动验证 API 正常 | `task-guide/desktop/preview-server.js` |
| 实机联调 | ⏳ 需你本地完成 | 手机装机 + 电脑连接 |

**最快看到效果的方式**：直接跑「电脑端预览」，1 分钟看到挂件 UI（用 mock 数据）。

---

## 1. 电脑端预览（最快，1 分钟看 UI）

无需装任何东西，Node 已有。

```bash
cd "D:/idol/X/Z/Q/资料/其他/For-workBoddy-work/task-guide/desktop"
node preview-server.js
```

浏览器打开 **http://localhost:3000** → 看到毛玻璃挂件 UI：
- 追踪任务卡片（带当前步骤 + 属性 + 进度条）
- 今日完成进度条
- 下一个提醒倒计时
- 习惯打卡状态
- 顶部三按钮：📌置顶 / 🖼普通 / 📥托盘（预览模式仅演示，真效果需 Tauri）

> 这是预览模式，用 mock 数据演示 UI。真正连接手机数据见第 3 节。

---

## 2. 电脑端正式版（Tauri 真挂件，可选）

想要「钉在所有窗口之上」「最小化进托盘」「开机自启」这些真效果，需装 Rust。

### 2.1 装 Rust（首次）
1. 下载 https://rustup.rs/ 的 rustup-init.exe，双击安装（全程回车默认）
2. 装 Microsoft C++ Build Tools（搜 "Visual Studio Build Tools"，勾选"使用 C++ 的桌面开发"）
3. 重开 CMD，`rustc --version` 能出版本号即装好

### 2.2 装 Tauri CLI + 跑起来
```bash
cd "D:/idol/X/Z/Q/资料/其他/For-workBoddy-work/task-guide/desktop"
npm install            # 装 @tauri-apps/cli
npm run tauri dev      # 首次会下载 + 编译 Rust 依赖，可能 10~20 分钟
```
编译完成后会自动弹出真挂件窗口（透明、置顶、可拖动）。

> ⚠️ Rust 首次编译很慢（要编译几百个 crate），后续改动编译很快。
> ⚠️ `desktop/icons/icon.png` 目前缺失，`tauri build` 打包时会报错。`tauri dev` 不影响。打包前放一张 512×512 PNG 到 `desktop/icons/icon.png` 即可。

---

## 3. 手机端（Android Kotlin）

### 3.1 装 Android Studio（首次）
1. 下载 https://developer.android.google.cn/studio （国内镜像）
2. 安装时勾选 Android SDK、Android SDK Platform 34、Android Virtual Device
3. 首次启动会自动下载 SDK，等它装完

### 3.2 打开工程编译
1. Android Studio → **Open** → 选 `task-guide/mobile/` 文件夹
2. 等 Gradle Sync 完成（首次会下载 Gradle 8.5 + 所有依赖，可能 10~30 分钟，看网速）
3. 连上你的 vivo 手机（开发者选项 → USB 调试打开）
4. 点 ▶️ Run → 编译并安装到手机

> ⚠️ `gradle-wrapper.jar` 我无法生成（二进制）。Android Studio 打开工程时会**自动补全**它。如果 Sync 报缺 wrapper，菜单 **File → Sync Project with Gradle Files** 一次即可。
> ⚠️ Ktor 在 Android 上跑服务器依赖较多，首次编译要拉取 `io.ktor:*` 系列，确保网络通畅（可用镜像）。

### 3.3 手机端验证
装好后打开 App：
- 点右下角 + 添加任务 → 任务列表出现
- 点任务进详情 → 添加步骤 → 开始追踪 → 追踪中 tab 看到卡片
- 设置页能看到「同步服务器」地址（如 192.168.43.1:8899）—— 这是电脑端要连的地址
- 桌面长按 → 添加小部件 → 选「任务指南 · 追踪」

---

## 4. 双端联调（手机开热点给电脑）

1. 手机端 App 已运行，设置页记下地址（如 `192.168.43.1:8899`）
2. 手机开热点，电脑连这个热点
3. 电脑端（Tauri 真版或预览版都行）顶部输入框填 `192.168.43.1:8899` → 点「连接」
4. 连接成功后：
   - 电脑挂件显示手机的追踪任务
   - 手机添加任务 → 电脑实时出现
   - 电脑推进步骤 → 手机实时更新

> 预览版（Node）连接是 mock，不会真连手机。要真连手机数据必须用 Tauri 版。

---

## 5. 已验证项（AI 在沙箱里跑过的）

- ✅ `db/schema.sql` 用 sqlite3 建库成功，5 张表 + 索引 + 约束全部正确
- ✅ 测试数据：5 种任务类型、追踪状态机、步骤、习惯打卡、软删除、唯一约束全部验证
- ✅ 关键查询：主列表（不含已完成）、追踪中任务+当前步骤 join、归档区、习惯连续天数
- ✅ Node 预览服务器启动成功，5 个 mock API 全部返回正确 JSON，advance_step 状态变更验证通过
- ✅ 前端 app.js 逻辑（环境检测、invoke/fetch 兼容、渲染、事件绑定）代码完整

## 6. 需你本地编译验证的（AI 无法在沙箱做）

- ⏳ 手机端 Kotlin 编译（需 Android Studio + SDK）
- ⏳ 电脑端 Rust 编译（需 Rust 工具链）
- ⏳ 实机 APK 安装 + vivo 小部件显示
- ⏳ Tauri 真挂件窗口效果（置顶/透明/托盘）
- ⏳ 双端局域网同步实测

---

## 7. 已知风险 / 可能需微调

| 风险点 | 说明 | 应对 |
|---|---|---|
| Kotlin 编译报错 | AI 无法编译验证，可能有小语法/API 问题 | 按 Android Studio 报错逐条修，通常是 import 缺失或 API 版本差异 |
| Ktor CIO 在 Android 兼容 | 理论可行，个别机型可能需换 Netty 引擎 | 改 `app/build.gradle.kts` 把 `ktor-server-cio` 换 `ktor-server-netty` |
| Tauri 2 API 变动 | Tauri 2 还在演进，个别 API 可能调整 | 按 `cargo` 报错查 Tauri 2 文档 |
| vivo 后台杀进程 | OriginOS 可能杀 SyncService 前台服务 | 设置 → 电池 → 允许任务指南后台运行 + 自启动 |
| gradle-wrapper.jar 缺失 | AI 无法生成二进制 | Android Studio 打开自动补全，或命令行 `gradle wrapper`（需先装 gradle） |

---

## 8. 错误处理（你的要求）

按需求文档第 7 节，反复出现的错误要总结成报告。建议在 `task-guide/` 下建 `BUG-REPORT.md`，每解决一个顽固 bug 就记一条（现象 / 原因 / 解决），避免重复踩坑。首次编译遇到的报错都记进去。

---

## 文件结构速查

```
task-guide/
├── README.md                  # 项目总览
├── 运行指南.md                 # ← 本文档
├── BUG-REPORT.md              # 错误记录（你维护）
├── docs/
│   ├── 技术方案.md
│   └── 同步协议.md
├── db/schema.sql              # 统一数据库（已验证）
├── mobile/                    # 手机端 Kotlin（20+ 源文件）
│   ├── app/src/main/java/com/taskguide/app/
│   │   ├── TaskGuideApp.kt          # Application 入口
│   │   ├── MainActivity.kt          # Compose + 导航
│   │   ├── data/ (model/db/repo)    # 数据层 + Repository + 追踪状态机
│   │   ├── notify/                   # WorkManager 提醒 + 通知 + 开机
│   │   ├── server/                   # Ktor HTTP+WS 服务器 + mDNS
│   │   ├── widget/                   # 桌面小部件
│   │   └── ui/                       # Compose 各界面
│   └── app/src/main/res/             # 布局/主题/图标/字符串
└── desktop/                   # 电脑端 Tauri + Node 预览
    ├── src/ (lib.rs, main.rs, sync.rs)  # Rust 后端
    ├── ui/ (index.html, style.css, app.js)  # 毛玻璃前端
    ├── preview-server.js       # Node 预览（已验证）
    ├── Cargo.toml, tauri.conf.json
    └── package.json
```
