# 任务栏（TaskBar）—— 运行指南 v3

> **手机 + 电脑 双端，原神式任务追踪**。  
> 单任务可"追踪→拆解步骤→推进→完成"，挂件长驻桌面。

---

## 0. 项目结构

```
task-guide/
├── README.md             # 介绍
├── BUG-REPORT.md         # 问题追踪
├── 运行指南.md            # 本文件
├── db/
│   └── schema.sql        # v3 统一 schema（手机+电脑共用）
├── desktop/              # 电脑客户端（Tauri 2 / Rust + Web 前端）
│   ├── Cargo.toml
│   ├── tauri.conf.json
│   ├── src/              # Rust 后端（lib.rs / main.rs / sync.rs）
│   ├── ui/               # Web 前端（HTML/CSS/JS）
│   ├── preview-server.js # Node 预览服务器（不开 Tauri 也能看 UI）
│   └── icons/
├── mobile/               # 手机客户端（Android / Kotlin + Compose）
│   ├── settings.gradle.kts
│   ├── app/
│   │   ├── build.gradle.kts
│   │   └── src/main/
│   │       ├── AndroidManifest.xml
│   │       ├── java/com/taskbar/app/    # 源码
│   │       │   ├── TaskBarApp.kt        # Application
│   │       │   ├── MainActivity.kt
│   │       │   ├── data/{model,db,repo}
│   │       │   ├── server/              # Ktor HTTP+WS 同步服务器
│   │       │   ├── notify/              # 通知 + WorkManager
│   │       │   ├── widget/              # 桌面小部件
│   │       │   └── ui/                  # Compose 界面
│   │       └── res/                     # 资源（字符串/主题/小部件）
└── docs/                 # 技术方案、协议
```

---

## 1. 数据库 schema（v3）

**所有数据最终落到一个 SQLite 文件**（手机端 `databases/taskbar.db`，电脑端 `%APPDATA%/com.taskbar.desktop/taskbar.db`）。

`db/schema.sql` 是双端共用的真值源。表：

| 表 | 作用 |
|---|---|
| `tasks` | 任务（含 4 分类 category 字段、deadline、追踪状态、积分） |
| `steps` | 任务的拆解步骤（仅追踪中的目标类任务可见） |
| `snooze_log` | 延迟历史（统计拖延、提醒策略学习） |
| `settings` | 全局设置（追踪上限、提醒提前量、积分等） |

> v3 新增：  
> - `tasks.category` 映射为 4 类（`daily` / `goal` / `time-limited` / `once`）  
> - `settings.tracking_max`（默认 3，可在设置里调）  
> - `settings.emergency_eta_short_pct`（默认 30，即 3/10）和 `settings.emergency_eta_long_hours`（默认 36）  
> - `settings.pairing_url` 持久化手机端地址

---

## 2. 桌面端（Windows）

### 2.1 预览（Node 模拟后端）

```bash
cd task-guide/desktop
node preview-server.js
# 浏览器打开 http://localhost:3000
```

预览服务器模拟了完整的 Tauri 后端 API：任务、追踪、积分、设置、紧急提醒、配对，全部可用。
**不依赖 Rust 工具链**，直接看 UI。

### 2.2 真客户端（Tauri 编译）

```bash
# 一次性：装 Rust + tauri-cli
# 参考：https://tauri.app/start/prerequisites/

cd task-guide/desktop
npm install        # 装 @tauri-apps/cli
npm run dev        # 开发模式（带热重载）
npm run build      # 出 .msi / .nsis 安装包
```

> **首次编译慢**（10-20 分钟），因为要拉 WebView2 运行时 + Rust crates。

### 2.3 桌面端功能

- **默认打开 = 任务总览**（4 分类：每日/目标/限时/小任务）
- 顶栏：标题、积分、设置、窗口模式（置顶/普通/托盘）、折叠
- 侧栏：总览、追踪中、今日、习惯、仓库
- 单条任务可追踪 / 取消追踪 / 标记完成 / 上下文菜单删除
- 紧急任务弹窗（≤48h 按 3/10 提醒，>48h 剩 36h 提醒）
- 挂件模式 = 右下角 340×76 悬浮条，点击展开回全屏
- 配对：输入手机 IP:8899 → 自动连 → 持久化到磁盘

---

## 3. 手机端（Android）

### 3.1 编译

```bash
cd task-guide/mobile
# 用 Android Studio 打开整个 mobile/ 目录
# 或命令行：
./gradlew assembleDebug
# 产物：mobile/app/build/outputs/apk/debug/app-debug.apk
```

> **手机需开启"未知来源安装"**。vivo 在 设置→安全→更多安全设置→安装未知应用 里授权。

### 3.2 手机端功能

- **默认打开 = 任务总览**（4 分类，与桌面同源 schema）
- 新建任务表单必选分类（4 选 1，互斥）
- 任务详情：步骤拆解、奖励区、追踪目标/标记完成/取消追踪
- 紧急任务：≤48h 按 3/10 提醒，>48h 剩 36h 提醒
- 桌面小部件（AppWidget）：4×2 卡片，显示当前追踪任务 + 步骤推进按钮
- 局域网同步：Ktor HTTP+WS 监听 8899 端口，桌面通过同 WiFi 连接

### 3.3 同步流程

1. 手机开热点 / 连 WiFi（同一局域网）
2. 桌面端设置页输入"手机 IP:8899" → 点连接
3. 桌面把这条 URL 持久化到 `~/.taskbar/pairing.json`
4. 之后只要在同一网络，桌面后台 WS 自动重连
5. 双方任意一边点"解除配对" → 重新走配对流程

---

## 4. 紧急提醒（双端一致）

| 任务剩余时长 | 提醒时机 |
|---|---|
| ≤ 48h | 剩余时长的 3/10（即 30%） |
| > 48h | 剩 36h 时 |

可在双端设置页调整（桌面在 ⚙ 设置 / 手机在设置页）：
- 关闭总开关
- 调整 ≤48h 百分比（默认 30）
- 调整 >48h 提前小时数（默认 36）

---

## 5. 积分系统

- 每个任务创建时默认 `reward_points = 10`（可在 v3 schema 里改）
- 完成 +1 次：自动累加到 `settings.total_points`
- 桌面顶栏 / 手机顶栏都显示当前积分
- 统计与排行榜：v4 计划

---

## 6. 故障排查

### 桌面看不到数据
- 检查 `preview-server.js` 是否在跑
- 浏览器硬刷新（Ctrl+Shift+R）

### 桌面 Tauri 编译失败
- `cargo clean && cargo build --release`
- 检查 Rust 版本 ≥ 1.74

### 手机收不到提醒
- vivo/小米要手动给"自启动"和"电池优化白名单"权限
- 紧急提醒走系统通知，权限被禁用就收不到

### 同步不上
- 双方要同一 WiFi（不要用访客网络）
- 防火墙允许 8899 端口
- 桌面端看设置页"配对状态"显示的 IP 是否正确

---

## 7. 版本

- 当前：v3.0
- 下一版计划：v3.1（多用户 / 云备份 / 桌面插件）
