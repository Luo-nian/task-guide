# Debug 日志机制（每版本一份）

> 目的：boss 反复提到的同一个 bug，不用重新从头排查 —— 直接读日志里的「已试方案 / 结论 / 下一步」。
> 同时让反复出现的 bug 有据可查，优先级自然抬升。
> 建：2026-09-12（v5.15.20 流程需求）

## 为什么是「索引 + 详表」两层

boss 担心每次读日志吃 token。所以拆成两层，常态开销≈0：

| 层 | 文件 | 体积上限 | 何时读 |
| --- | --- | --- | --- |
| 索引 | `BUG-INDEX.md` | **≤ 2KB**（约 20 行） | 只在**触发词命中**时读 |
| 详表 | `debug-v<版本>.md` | ≤ 6KB / 份 | 只在**索引命中某条**时读那一份 |

## 读取规则（硬性）

1. **默认不读**。普通对话、新需求、无关问题 —— 不读索引，不读详表。
2. **触发词命中才读索引**。boss 话里出现下列任一情况：
   - 明确说「又 / 还是 / 上次那个 / 之前提过 / 没修好 / 一直都 / 反复」
   - 说「bug / 不行 / 有问题 / 报错 / 失效 / 没生效 / 白屏 / 闪」
   - 提到某个**已知症状关键词**（见索引的「症状关键词」列）
3. **命中索引某条 → 只读那一条对应的详表文件**，不批量读。
4. 每条详表控制在 ≤ 6KB；超了就拆「当前版本 / 历史版本」。

## 写入规则（硬性）

每个版本收尾时（commit 前）必须在该版本详表里补：

- **已解决**：一句话根因 + 修法落点（`文件:行号`）+ 指向需求文档
- **未解决 / 待验证**：现象 → 已试方案（逐个列出，含失败的那个）→ 当前结论 → 下一步 → **症状关键词**

同时在 `BUG-INDEX.md` 同步：
- 新出现且未解决 → 加一行
- 已解决 → **从「未解决」区删掉**，挪到「已解决归档」（只留一行，不写细节）

**索引只保留未解决的**，所以它会随时间自然变短，不会无限膨胀。

## 命名

- 详表：`debug-v5.15.20.md`（跟 `requirements/项目需求-v<版本>.md` 同号）
- 一个版本一份；跨版本没解决的 bug，在**最新版本**的详表里续写，索引里更新「首现版本 / 最新进展版本」

## 目录

```
debug/
├── README.md          ← 本文件（机制说明）
├── BUG-INDEX.md       ← 极简索引，唯一入口
├── debug-v5.15.19.md  ← 每版本详表
└── debug-v5.15.20.md
```

## 已解决归档（从 BUG-INDEX 移来，2026-09-20）

> **`BUG-INDEX.md` 只留未解决 / 待验证项**，保持"读一页就能判断"的轻量（现 ~4KB）。
> 已修的历史根因整表放这里 —— 查「这个 bug 以前修过没有」先搜本表，`Ctrl+F` 关键词即可。

| ID | 一句话 | 版本 |
| --- | --- | --- |
| C-031 | ⭐ **追踪名额"假满"**：解锁完整版+上限3只能追1个。真机 DB 实证：`track_status='tracking'` 5 行中 4 行是 `deleted=1` 的历史占坑 → 拦截计数没排 `deleted` 数出 5≥3。**修：v5.27.1 起 countTracking/observeTrackingCount 全带 `deleted=0`**（真机复验计数=1，UI 一致）。⭐ 教训：配额/上限类 `COUNT(*)` 必须与列表口径同步过滤软删除；透明化只是止痛，口径对齐才是根治。详见 [v5.28.0](./debug-v5.28.0.md#c-031--追踪名额假满解锁完整版上限-3却只能追-1-个定案) | v5.28.0 |
| C-030 | ⭐⭐ **提醒到点根本不执行（真根因）**：任务提醒用 `OneTimeWorkRequest`，而它本质是"不精确的后台任务"——实测「健身」的提醒**逾期 15 小时仍未执行**（state=0 / run_attempt=0；手机一直开着、App 在电池白名单、未 Doze、权限已声明）。**修：改用 `AlarmManager.setExactAndAllowWhileIdle`（与起床提醒同一套）+ 新增 `ReminderAlarmReceiver` + 把提醒逻辑抽成 `fireNow()` 两路共用**。⚠️ 教训：**"逻辑修好了"不等于"功能修好了" —— 排程机制本身也要验证**。详见 [v5.18.5](../requirements/项目需求-v5.18.5.md) | v5.18.5 |
| C-029 | ⭐⭐ **提醒系统四连（v5.18.4，真机 A/B 实测）**：①每日任务被当"已过期未完成"→每次冷启动立即提醒；②裸 `enqueue()`+每次冷启动都跑 → 同一任务堆 20 条，开几次 App 响几次；③⭐⭐ **`observeMainList()` 的 SQL 自带 `track_status != 'done'`** → "昨天完成的每日任务"在 SQL 层就被滤掉 （每日折算只在 UI 层）→ **今天的提醒永远排不上**；⚠️ **排程/后台任务不能复用 UI 的列表查询**；④`observeByUuid` 不过滤 `deleted` 且删除路径没有 `cancel` → 删掉的任务到点还弹提醒。详见 [v5.18.4](../requirements/项目需求-v5.18.4.md) | v5.18.4 |
| C-023 | **AlarmManager 类功能"突然失效"**：「每日提醒」的闹钟只在 BOOT_COMPLETED 恢复 → **重装/强停/被清**后台后闹钟全丢，不重启手机就再也不响。**修法：应用冷启动也 restore 一次**。详见 [v5.15.27](./debug-v5.15.27.md#c-023) | v5.15.27 |
| C-024 | **收起/展开导致页面抖动**：`toggleCatGroup` 调了 `render()` 整页重渲染（重建 innerHTML → 滚动重置 + 闪）。**修法：就地切 class，绝不重渲染**。详见 [v5.15.27](./debug-v5.15.27.md#c-024) | v5.15.27 |
| C-026 | ⭐ **开关"点不动"**：`Switch(checked = prefs.getBoolean(...))` 用的是**普通 val 不是 State** → 关掉时无状态变化 → 不重组 → 视觉纹丝不动（prefs 其实已写）。**修法：改成 `mutableStateOf`**。详见 [v5.15.27](./debug-v5.15.27.md#c-026) | v5.15.27 |
| C-025 | **预览里的 SMIL（`<animate>`）会被渲染器丢掉** → boss 看到静态图 = "做了等于没做"。**改用 CSS 关键帧**；CSS 动 SVG 要 `transform-box:fill-box` + `transform-origin:center`。详见 [v5.15.29](./debug-v5.15.29.md#c-025) | v5.15.29 |
| C-027 | ⭐ **CSS 注释收尾写成 `-->`** → 一次吞掉其后 4 条规则，"有的生效有的没生效"。CSS 只能 `*/` 收尾。详见 [v5.15.29](./debug-v5.15.29.md#c-027) | v5.15.29 |
| C-028 | ⭐⭐ **删了 HTML 元素、JS 顶层还在绑事件** → `TypeError` 砍断顶层执行链 → `DOMContentLoaded` 没注册 → **整页不启动**（问候语/积分停在静态死值）。判据：手动调 `startApp()` 报 `Cannot access 'x' before initialization`（TDZ）。详见 [v5.15.29](./debug-v5.15.29.md#c-028) | v5.15.29 |
| C-012 | **补丁"静默失败"**：后台跑补丁 + `tail -N` 看日志 → assert 报错被截掉，以为改了其实一行没动；**"编译成功"≠"改动生效"**，UI 改完必须真机确认新元素出现。详见 [v5.15.24](./debug-v5.15.24.md#c-012) | v5.15.24 |
| C-013 | **补丁插错函数**：拿通用变量名（`listState`）当锚点，文件里有两处 → 插到了主页。锚点必须唯一；`count=1` 的替换先 `grep -c` 确认。详见 [v5.15.24](./debug-v5.15.24.md#c-013) | v5.15.24 |
| C-007 | **adb 连不上副机**：真凶是 `adb server` 被沙箱回收（每条命令都 `daemon not running`）+ 无线调试端口漂移。修复：`wmic` 拉起常驻 server（监听默认 5037）+ 端口扫描 + `tcpip 5555` 固化 + `tools/adb-connect.sh` 四级兜底。详见 [v5.15.24](./debug-v5.15.24.md#c-007) | v5.15.24 |
| C-008 | **streak 有三份实现且规则不同**（只修了 1 份）→ 抽 `streakOf` 统一。详见 [v5.15.24](./debug-v5.15.24.md#c-008) | v5.15.24 |
| C-009 | **日历把已打卡习惯标"未完成"**：日历看 `trackStatus`，而习惯打卡不改它 → 加 `doneOf` 回调，与列表统一走 `todayCheckedHabits`。详见 [v5.15.24](./debug-v5.15.24.md#c-009) | v5.15.24 |
| C-010 | 加强度分后习惯行文案换行 → 去掉冗余文案 + `maxLines=1`。详见 [v5.15.24](./debug-v5.15.24.md#c-010) | v5.15.24 |
| C-001 | **双端同步单向失效真凶 = 系统代理(127.0.0.1:7892)劫持 reqwest HTTP（502）**；WS 不走代理所以「已连接」是假象。修复：HTTP 全改 no_proxy 直连 + 桌面全量推送 + sync-debug.log。详见 [v5.15.22](./debug-v5.15.22.md#c-001) | v5.15.22 |
| C-002 | **改样式没生效：文件尾部还有同名 `.level-card !important` 覆盖** → 改 CSS 前先 grep 全部定义点。详见 [v5.15.22](./debug-v5.15.22.md#c-002) | v5.15.22 |
| A-006 | **点分类列表空但实际有任务**（boss 第二次反馈）：后端只按 `category` 字段查，前端却按 `type` 推断 → 自定义分类被漏 | v5.15.21 |
| A-007 | **今日待办任务全不见**：全局 `.vh-hint` 的 `flex-basis:100%` 在 column 容器里 = 占满整列高度 | v5.15.21 |
| A-008 | 侧边栏角标改大没生效：`.nav-count` 有**两处定义**，生效的是靠后那处 | v5.15.21 |
| A-009 | 两处旧 CSS 错误（选择器丢失 + 多余 `}`）→ 已用逐行解析脚本确认括号配平 | v5.15.21 |
| A-010 | 手机端次数任务的「次数」存不进：`createTask` 只给里程碑保留 `target` | v5.15.21 |
| A-001 | 闪屏：每 15s 无条件全量重绘 + `#app.fading` 死代码 | v5.15.18 |
| A-002 | 手机端无「已连接」UI，只有历史「已配对」 | v5.15.19 |
| A-003 | 自动扫描只跑有限轮就永久停手（看门狗 + mDNS 自适应） | v5.15.19 |
| A-004 | 打卡键被裁成「扌」：`IconButton` 强制 40dp，改用 `PressPill` | v5.15.17 |
| A-005 | 双端同步 4 个根因（serde default / snake_case / Upsert id / 误判删除） | v5.15.7 |
