# 交接文档 · v5.15.22（✅ 已完结 2026-09-13）

> 写给"下一次开新会话的我"。**本版 14 项（含追加 M5b/D4/D5）已全部完成、编译、部署并验证。M11 真凶 = 系统代理劫持 reqwest（debug-v5.15.22.md C-001）。M8/M10 代码已修，交互留 boss 实测。**
> 需求全文：`requirements/项目需求-v5.15.22.md`　｜　bug 索引：`debug/BUG-INDEX.md`

---

## ⚠️ 接手第一件事：先编译（改动没编译过）

本轮改了 5 个文件但**一次都没编译**。动手前先跑编译，确认我之前的改动没语法错：

```bash
# 手机端（约 4~6 分钟）
export JAVA_HOME="D:/taskbuild2/jdk17/jdk-17.0.20.1+1"
export PATH="$JAVA_HOME/bin:/d/taskbuild2/gradle-dist/gradle-8.5/bin:$PATH"
cd "D:/taskbuild2/mobile" && gradle :app:assembleRelease --offline -x lint
```

**同步源码到编译副本**（源工程 → 副本，改完必须重跑，否则编译的是旧代码）：
```bash
cd "D:/idol/X/Z/Q/资料/其他/For-workBoddy-work/task-guide"
cp mobile/app/src/main/java/com/taskbar/app/ui/Screens.kt \
   "D:/taskbuild2/mobile/app/src/main/java/com/taskbar/app/ui/Screens.kt"
# 同理：TaskCalendar.kt / TaskDetailScreen.kt / TaskViewModel.kt /
#       data/repo/TaskRepository.kt
```

---

## ✅ 本轮已完成（4 项，均为代码改动，待编译+真机验证）

### M10 🐛「学英语10分钟 / 隔天喝水8杯」点完成只弹积分、任务不动 — **根因已根治**

**根因（两层）**：
1. `TaskRepository.completeTask` 读的是 **DB 原值**。而每日任务（`category='daily'`）跨天后，
   `normalizeDailyReset()` 只在**读取时**把它折算回"今天的待办"（**不落库**）→ DB 里它仍是 `DONE`
   → 走到 `if (task.trackStatus == DONE) return` **静默提前返回**（不加积分、不改状态）。
2. `TaskViewModel.completeTask` 里弹窗是**无条件**设置的（只要 task 不为 null）→
   即使 repo 静默 return，界面照样弹"获得积分" → 用户以为完成了。

**改法**：
- `TaskRepository.kt`：`completeTask` 开头先 `normalizeDailyReset(raw, todayStart())`
  （新增 `todayStart()` 辅助函数），让跨天的每日任务能真正被"再完成一次"。
- `TaskViewModel.kt`：弹窗条件改为 **`points > pointsBefore`**（积分真的增加才弹），
  且显示的是实际增量 `points - pointsBefore` 而非任务标称值。

**验证数据**：电脑端 DB 里「学英语 10 分钟」`done=0 / st=pending` 却显示在今日待办
（= normalizeDailyReset 折算的结果），「隔天喝水 8 杯」`done=1`（其实早已完成）——两者都符合上述推理。

### M1 历史任务**默认日历形式**
`Screens.kt` → `HistoryScreen` 的 `calView` 初值 `false` → **`true`**。
（`AllTasksScreen` 保持默认列表，boss 只要求历史页）

### M2 日历角标太小 + 位置偏下
`TaskCalendar.kt` → 角标从"日期下方居中"改为**格子右上角**（`Alignment.TopEnd`），
字号 `9sp → 10.5sp`，底色透明度 `0.5 → 0.72`。

### M8 习惯周期 bug：每天→隔天→每周 会重置回"每天"
**根因（很隐蔽）**：`HabitCycleEditor` 里 `mode/everyN/selectedDays` 都是 `remember(rule)`。
点「每周」时 `serializeWeekly(selectedDays)`，而 `selectedDays` 此时**是空集**
（因为它按 rule 解析，`every2d` 解析不出周几）→ `serializeWeekly(空)` 返回 **`null`**
→ `parseRule(null)` = `"daily"` → **`remember(rule)` 的 key 从 `every2d` 变成 `null`，三个 state 全部重建** → mode 被重置成 daily。
> 所以"从每天直接点每周"看着正常 —— 那时 rule 本来就是 `null`，key 没变。

**改法**：点「每周」时若 `selectedDays` 为空，先填默认 `setOf(1,3,5)`，保证 rule 真变成 `weekly:1,3,5`。

---

## ⬜ 剩余待做（10 项）

| # | 需求 | 落点 / 线索 |
| --- | --- | --- |
| **M11** | 🐛 **双端任务没完全同步** | 见下方「同步缺口」。副机 adb 现在连不上，**优先恢复真机连接**才能取手机端数据 |
| **M3** | 历史任务要把**已逾期未完成的每日任务**算进去（12/13 日没数量） | `Dao.observeArchive()` 只查 `track_status='done'`。需合并"未完成的 daily 任务"。**注意**：电脑端 9/12 有 1 条、9/13 有 11 条已完成 → 手机端没数据显示的是**同步问题**，别只改数据源 |
| **M4** | 页面切换 UI（左右滑动） | `NavHost` → `HorizontalPager`。**属结构性改动**，建议先跟 boss 确认范围（只滑 3 个主 tab？详情页要不要也能滑？） |
| **M5** | 追踪后**取消追踪键要有涟漪 UI** | 电脑端有 `@keyframes rippleBreath`（style.css）；手机端无涟漪，需在 Compose 里做（`infiniteTransition` + 扩散圆环） |
| **M6** | 追踪键与完成键**列对齐**（boss 说上轮改完还是没对齐） | 上轮已改成等宽 `PressIcon`；真机复核 `TrackTaskCard` 里两键的实际渲染尺寸，可能需要强制同尺寸容器 |
| **M7** | 优先级 高/中/低 **按键颜色要区分** | `TaskDetailScreen` 编辑页的 `FilterChip` 三档同色 → 按优先级给不同色 |
| **M9** | 日历里点任务**只能看详情**（不能编辑/完成/无功能键，防刷分） | 需给详情页加"只读模式"（新路由或参数），只渲染信息不渲染 `.dv-acts` 等操作区 |
| **D1** | 电脑端等级栏**还是太扁**（图1） | 上轮已改 `.level-card`（padding 18 / min-height 96 / 图标 64）。boss 仍觉扁 → **先确认他看的是不是新 exe**，再看是不是 `.dash-greet-ring` 那条 |
| **D2** | 「追踪任务」键图标换成追踪页/侧边栏同款（图2） | 现在是 `play` 三角。侧边栏「追踪」上轮已改为 `goal` 图标、追踪页用 `ic_track_fill` → 统一 |
| **D3** | 「追踪中」**统计卡右上角图标**也要换（图3） | `index.html` 统计卡区（`data-icon="crosshair"`）。boss 明确：**只换这一个图标** |
| **D4** | 电脑端**加 UI**（boss 让我自己看着加） | 自主发挥。boss 补充：*"如果你是为了减少cpu占用那当我没说"* → 别加实时动画，优先静态视觉丰富度 |

---

## 🔍 已定位的关键线索

### 同步缺口（M11 的证据）
电脑端 `D:\Desktop\TaskBar-v4.10\taskguide.db` 里已完成任务的 `done_at` 分布：

| 日期 | 条数 |
| --- | --- |
| 2026-09-13 | 11 |
| 2026-09-12 | 1 |
| 2026-09-11 | 4 |
| 2026-09-10 | 10 |

→ **电脑端 12/13 日是有数据的**，boss 说"12 13日没有任务数量"看的必然是**手机端** → 手机端缺数据 = 同步问题（M11），不是日历逻辑问题。

### 手机端任务数据怎么取（不用 root）
手机端有 HTTP API：`GET http://<手机IP>:8899/api/sync/full` 返回全量任务 JSON。
（另有 `/api/ping`、`/api/pair/status`、`/api/sync/incremental`、`POST /api/sync/changes`）
> 上一轮用 `10.200.37.105:8899` 取数超时；本次 boss 给的新地址是 `10.112.227.105:34011`（adb 端口），
> HTTP 端口仍是 **8899**。

### 副机 adb 连接（当前失败）
```
boss 给的：配对码 325480 / 地址 10.112.227.105:34011
实测：connect → offline；pair → protocol fault
```
**排查顺序**：① 手机无线调试界面里的**配对端口**通常与连接端口不同（形如 `10.x.x.x:41234`），
boss 给的可能是**连接端口** → 需要问他要"使用配对码配对设备"弹窗里那个地址；
② 或先 `adb disconnect` 清掉 offline 记录再重连；
③ mDNS 自动发现：`adb mdns services`（注意：**手机息屏后 mDNS 广播会停**）。

---

## 🧰 环境速查

| 项 | 值 |
| --- | --- |
| 源工程 | `D:\idol\X\Z\Q\资料\其他\For-workBoddy-work\task-guide` |
| 编译副本 | 手机 `D:\taskbuild2\mobile`；桌面 `D:\taskbuild2\desktop-src` + `desktop-target` |
| 工具链 | `D:\taskbuild2\jdk17\...`、`gradle-dist\gradle-8.5`、`rustup`、`w64devkit\w64devkit\bin`、`cargo-home` |
| adb | `C:\Users\Hasee\.workbuddy\tools\platform-tools\`，**用 `ANDROID_ADB_SERVER_PORT=5038`**（5037 被占） |
| 交付目录 | `D:\Desktop\TaskBar-v4.10\`（桌面端 exe + apk + 调试启动器 `任务栏-调试模式.cmd`） |
| 真实桌面 | `D:\Desktop`（注册表重定向，`C:\Users\Hasee\Desktop` 是残留） |

### 两个必备的前端检查（每轮改完必跑）
```bash
node --check "D:/taskbuild2/desktop-src/ui/app3.js"      # JS 语法
# CSS 括号配平（脚本见 debug-v5.15.21.md 的 D-004 段，逐行解析报 depth<0）
```

### 已知的坑（本轮踩过）
- **`flex-basis:100%` 在纵向容器里 = 占满整列高度**（v5.15.21 R1 把任务全挤没了）
- **CSS 同名规则可能有多处定义，生效的是靠后那处**（`.nav-count` 就是这么坑的）
- **`@Composable` 注解不可重复**：在已有注解的函数前插新 Composable 要小心
- **Kotlin top-level `private` = 文件私有**，跨文件用要改 `internal`
- 改完源工程**必须 cp 到编译副本**，否则编译的是旧代码

---

## 🎯 建议的下一步顺序

1. **先编译**（本轮改动未验证）
2. **恢复副机 adb 连接**（拿配对端口）→ 取 `/api/sync/full` 对比双端数据 → 定位 M11 同步缺口
3. 做 **M9**（日历只读，防刷分，逻辑独立风险低）
4. 做 **M7 / M6 / M5**（按键颜色 / 对齐 / 涟漪，都是 UI 局部改动）
5. 做 **M3**（历史数据源）+ **D1/D2/D3**（电脑端小改）
6. **M4**（页面滑动）+ **D4**（电脑端加 UI）—— 需要先跟 boss 确认范围
