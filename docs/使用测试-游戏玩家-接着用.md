# 使用测试 · 游戏玩家视角｜第二个月（22 岁 / 男 / 手游日活 / Lv.4 起）

> 接第一个月**继续用**，不注册、数据不过云。逐条去代码核对了 v5.23.0 声明。
> 标注：【已核实】+文件:行号 / 【推测】+依据。看不出写"看不到"。真机未实测（adb 连不上）。

## 1 第二个月第一印象
满一个月再打开，几十上百条数据列表/搜索还能扛；v5.23.0 的提醒终于"建了当天会响、改时间旧闹钟被撤"。但换机没导入、批量恢复能刷分这两个老伤还在，等级也快撞天花板。

## 2 时间线
- **第 2 月第 1 天**：面对上月几十条历史，today 列表仍把"每周三"任务每天顶上来（`observeMainListToday` 不过滤星期），看着乱。
- **第 1 周末**：批量归档+恢复清理。批量恢复把已完成项也按"逾期半价"扣 → 净赚分【已核实】`TaskViewModel.kt:204-208`/`TaskRepository.kt:381`。
- **第 2 周（连击疲劳）**：长连击反而怕断，断一天归零不能补卡；强度分算好却不显示【已核实】`TaskRepository.kt:418` streakOf 归零、`HabitStat` 无 UI 引用。
- **第 3 周（数据量）**：列表/日历翻月、历史近百行，"逾期未完成"刷屏且无搜索【推测】历史页无搜索框（前报 `Screens.kt`）。提醒这周稳了。
- **第 4 周（月末回顾）**：想看成长曲线，手机端只有今日进度、无积分流水【已核实】`ProfileScreen.kt`；电脑端首页三数字仍打架【推测】`app3.js` 首页统计。
- **月底（换机视角）**：导出在私有 `backups` 目录、无导入、授权码不进备份 → 换机全丢【已核实】`SettingsScreen.kt:958-977` 只 export、无 import。

## 3 问题清单
| # | 严重度 | 问题 | 依据 | 建议 |
| --- | --- | --- | --- | --- |
| 1 | 功能不可用 | 换机/重装**无导入**：导出仅私有目录、授权码不进备份→换机即全丢 | 【已核实】`SettingsScreen.kt:958-977` 仅 exportJson；需求未修#2 | 做导入+迁移目录+授权码入备份 |
| 2 | 数据错·可刷分 | 批量恢复整批套同一 `overdueHalf`：已完成项混进"逾期恢复"只扣半分，再完成净赚半分 | 【已核实】`TaskViewModel.kt:204-208`/`TaskRepository.kt:381` | 逐条判定是否逾期 |
| 3 | 数据错 | 双端同时改同一条按本机时钟 LWW 互相覆盖；积分按绝对值同步→离线分互丢 | 【推测】需求未修#3 / `SyncService` LWW | 版本向量/服务端时间 |
| 4 | 功能不可用 | 电脑端**无到点提醒**，仅 22:00 页内弹窗，`lib.rs` 无 alarm 调度 | 【已核实】`ui/app.js:1545` 仅 checkNightNotify；`src/lib.rs` 无 schedule | 桌面也排到点提醒 |
| 5 | 功能不可用·⚠️遗留 | "每周一三五/隔天"**今日列表不按星期过滤**：每周任务每天出现在今日/日历 | 【已核实】`Database.kt:69-82` 无 weekday 过滤；`nextOccurrenceFor` 仅用于提醒 `ReminderScheduler.kt:222` | 今日/日历按 `repeatRule` 过滤 |
| 6 | 数据错 | 恢复旧任务**静默掉级**，只有升级有弹窗 | 【已核实】`TaskViewModel.kt:191-194` 只判 `after.lv>before.lv` | 掉级提示+扣分预览 |
| 7 | 难用·连击疲劳 | 连续天数断一天归零、不能补卡；强度分无 UI | 【已核实】`TaskRepository.kt:418` / `HabitStat` 无引用 | 展示强度分/有限补卡 |
| 8 | 难用 | 历史被"逾期未完成"刷屏、无搜索、逐天展开 | 【推测】前报 `Screens.kt` 历史无搜索框 | 历史加搜索+只看已完成 |
| 9 | 功能不可用 | "提醒范围"死设置：选"仅电脑"手机端仍照响（不校验 scope） | 【推测】需求未修#5；`NotificationHelper.kt:136-200` 只按 strength 弹 | 按端过滤 |
| 10 | 难用 | 手机端无数值面板/积分流水，成长曲线对不出 | 【已核实】`ProfileScreen.kt` 仅今日进度 | 补趋势卡+积分明细 |
| 11 | 难用·困惑 | 电脑端首页三数字打架（待办/进度/今日剩余口径不一） | 【推测】前报 `app3.js` 首页统计 | 统一"今日"口径 |
| 12 | 数据错·无声失败 | 次数任务"可得 N 分"预览显整笔，实际分次给，预览≠实得 | 【已核实】`TaskDetailScreen.kt:773` 整笔 / `TaskRepository.kt:336-348` 分次 `per` | 预览改"每次+per，共 N 次" |
| 13 | 功能不可用·无声失败 | 桌面挂件「+」键无效、标题旧名「任务指南」 | 【推测】需求未修#7 | 修挂件 intent/标题 |
| 14 | 难用·成长感 | 等级天花板低 Lv.10=3200 分≈第45~55天登顶，之后只剩数字 | 【推测】`Models.kt` Levels（前报）；第2月正撞登顶 | 加登顶后内容/赛季 |

## 4 v5.23.0 修复核对
| # | 声明 | 结论 | 代码依据 |
| --- | --- | --- | --- |
| 1 | 增删改完成恢复后立即重排提醒 | ✅真修好 | 【已核实】`TaskViewModel.kt:136` `rescheduleOne`，调用 179/186/192/200/207/255 |
| 2 | 「延迟1天」真 24h（原+1ms） | ✅真修好 | 【已核实】`ReminderActionReceiver.kt:330` `24L*3600_000L`；非每日 `delayTask` 写库 :362 |
| 3 | 通知按钮 `dueAt!!` 空指针→判空 | ✅真修好 | 【已核实】`ReminderActionReceiver.kt:351-355` `base==null` 仅清通知 |
| 4 | 每日型 due_at 不被延迟平移 | ✅真修好 | 【已核实】`ReminderActionReceiver.kt:364-367` 每日走 `nextDailyOccurrence` 不写库 |
| 5 | `schedule()` 时刻已过不静默丢弃 | ✅真修好 | 【已核实】`ReminderScheduler.kt:127` `dueAt>now?dueAt:now+1500L` |
| 6 | 点通知直接打开那条任务 | ✅真修好 | 【已核实】`MainActivity.kt:83/120/149`+`NotificationHelper.kt:148` 带 task_uuid |
| 7 | 补 `USE_FULL_SCREEN_INTENT` | ✅真修好 | 【已核实】`AndroidManifest.xml:23` |
| 8 | 详情页删除二次确认 | ✅真修好 | 【已核实】`TaskDetailScreen.kt:82-95`（触发 :180） |
| 9 | 「每周一三五/隔天」真正生效 | ⚠️有遗留 | 【已核实】`ReminderScheduler.kt:81-118` 仅用于**提醒**；今日列表 `Database.kt:69-82` 不按星期过滤→每天仍显示 |
| 10 | 导出排除 `auth_secret` | ✅真修好 | 【已核实】`SettingsScreen.kt:966` `filterNot{key=="auth_secret"}` |
| 11 | 次数任务改累计推进 | ✅真修好 | 【已核实】`TaskRepository.kt:336-353` 满次数才归档、分次给分无刷分空间 |
| 12 | 次数任务可设提醒时间 | ✅真修好 | 【已核实】`TaskDetailScreen.kt:649-652/791/798` dueAt 不强制空、置 daily 重复 |
| 13 | 夜间米白底白字→固定深褐 | ✅真修好 | 【已核实】`UiFoundation.kt:714`+`Screens.kt:1716` 固定 `0xFF3B2A20` |

## 5 第二个月还会继续用 / 会不会买断
**会继续用**——提醒稳了、次数任务能累计，晚上游戏本前顺手清任务已成本能。但**不会马上买断**：
- 第一月就想要的"同时追多个"被免费 1 个挡住，本想第 2 周买；但第 1 周末发现**批量恢复能刷分**（#2）、**恢复会静默掉级**（#6），数值开始不可信→等级没意义，买单意愿掉。
- 第 3 周数据量上来后**历史无搜索、每周任务每天蹦**（#5/#8）很烦；月底**换机没导入**（#1）是硬伤——"数据不过云"反而成了风险，作者已知未修。
- 买断时点：**降到第 4 周犹豫，¥5~12 仍会买，但前提是先把 #1/#2/#6 修了**；≥20 元不买。否则宁可用免费版扛着，反正任务数量不限。

---
**回报**：问题 14 条；严重级——功能不可用 5（#1/#4/#5/#9/#13）、数据错 4（#2/#3/#6/#12）、难用/困惑 5（#7/#8/#10/#11/#14）、崩溃 0（合计"崩溃/数据错/功能不可用"9 条 ≥3 达标）。v5.23.0 修复核对：✅ 12 条、⚠️ 1 条（#9 提醒生效但今日列表不按星期过滤）、❌ 0 条。
