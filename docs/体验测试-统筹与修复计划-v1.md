# 体验测试统筹报告 v1（2026-09-22）

> 来源：4 份**代入式**使用测试报告（每份都按「这个身份**一个月**的使用与活动」走查）
> - `使用测试-大学生.md`（20 岁男，大二，安卓中端机为主）
> - `使用测试-大学生-一个月.md`（同一身份，**一个月时间线**专项）
> - `使用测试-游戏玩家.md`（22 岁男，手游玩家的日活玩家，对数值/准时极敏感）
> - `使用测试-程序员.md`（28 岁男，后端开发，看数据怎么存、怎么迁、权限与冲突）
>
> ⚠️ 测试者**只看截图与源码、没有实机点击**，所以每条都标了【已核实】（给了 文件:行号）
> 或【推测】。下面我把它们**按严重度重新排序**，并标注**我是否自己复核过**。

---

## 一、我本轮已经动手修的（2 条）

| # | 问题 | 来源 | 我做了什么 |
| --- | --- | --- | --- |
| 1 | **任务数据 + 配对密钥会被系统云备份带走**：`AndroidManifest` 里 `allowBackup="true"` 且无备份规则，而配对密钥 `auth_secret` 与任务**同一个库** → 与 App 宣称的「数据只在你自己的设备里、不经过云端」直接冲突 | 程序员报告 | 改为 `allowBackup="false"` + 新增 `res/xml/data_extraction_rules.xml`（云备份与换机迁移**全部排除**）。用户要备份请走 App 内导出 / 局域网同步 |
| 2 | **每日型提醒响过一次就再也不排下一次**：全项目唯一排程入口是「冷启动 / 开机」，闹钟触发后不重排 → 只有**每天都打开 App** 的人才能收到第二天的提醒（正好解释测试里「有两天没响」） | 大学生·一个月报告 | 在 `ReminderAlarmReceiver` 触发提醒后补排一次 `rescheduleAll`；下次冷启动会再校正 |

> 这两条都是**低成本、影响大**的：一条是隐私承诺的一致性，一条是「提醒到底靠不靠得住」（你一直最在意的那件事）。

---

## 二、严重：确认是真问题，但改动有风险 —— 建议下个版本专门做

| # | 问题 | 来源 | 为什么暂不动 |
| --- | --- | --- | --- |
| 3 | **手机端「次数任务」点一次就归档**：`completeTask` 只对 MILESTONE 走 `progress+1`，`category=='once'` 直接置 DONE（`TaskRepository.kt:326-338`），但扣分按"多次"减半（`:209-210`）；电脑端却有 `done_count` 计数 → **双端行为不一致 + 白扣一半分** | 游戏玩家 | 属于**任务语义**改动，会同时影响计分、同步与双端显示，必须配一轮回归测试 |
| 4 | **批量恢复扣分算错（可刷分）**：`Screens.kt:1929` 用「选中里有没有逾期任务」一个标志决定整批的扣分档，混选时已完成项也只扣半价（`TaskRepository.kt:361`） | 大学生·一个月 | 同上，涉及计分规则 |
| 5 | **恢复旧任务会静默掉级**：等级=积分，扣成负数也行（`TaskRepository.kt:704-707`），但只有升级有弹窗，掉级没有任何提示 | 大学生·一个月 / 游戏玩家 | 需要先定「等级能不能掉」这个产品决策 |
| 6 | **两端同时改同一条任务，冲突不可解释**：整条任务 LWW + 用**本机时钟**、无设备号兜底（`TaskRepository.kt:757-766`、`sync.rs:277-289`）；`delete` 变更不带时间戳 → 时钟快的一端永久赢、删除先后判不出来 | 程序员 | 要动同步协议（加设备号+Lamport/向量），是**架构级**改动 |
| 7 | **没有「导入」**：全仓无导入实现；导出落在 `getExternalFilesDir(null)/backups`（卸载即删、Android 11+ 文件管理器进不去，`SettingsScreen.kt:909-922`）；电脑端**完全没备份** | 大学生 / 程序员 | 「能恢复」是买断的门票之一；建议下版先做「导出到用户可选目录 + 导入回滚」 |

---

## 三、中等：影响体验，改动不大但要有取舍

| # | 问题 | 来源 |
| --- | --- | --- |
| 8 | **「每日任务刷新时间」是死设置**：两端 UI 都能改，但两端日界都写死本地 0 点（`lib.rs:201-209`、`TaskRepository.kt:106-111`），需求文档自己都记着「未接线」 | 大学生·一个月 / 程序员 |
| 9 | **通知权限被拒 = 一个月全哑**：`MainActivity.kt:82-87` 只在启动请求一次、回调为空；设置里没有任何权限状态或补救入口 | 大学生·一个月 |
| 10 | **「提前多久提醒」是死设置**：电脑端能选 15 分钟/1 小时/1 天并高亮（`app3.js:492-557`），但全项目没有任何消费点；手机端解析配置时把 `min` 丢掉（`Models.kt:131-147`），且手机新建任务根本没有时间项 | 大学生 |
| 11 | **电脑端首页三个数字打架**：进度 SQL 只认 `category='daily'`（`lib.rs:520-527`），而列表按 `!done` 计数（`app3.js:798,820`）→ 「1 项未完成 / 0%/0/0 / 今日剩余 0」同屏矛盾 | 大学生 / 游戏玩家 |
| 12 | **等级曲线太快**：Lv1→Lv5 只要 420 分，开发者注释自称「40+ 天登顶」，玩家实测第 45~55 天满级（3200 分）→ **满级后没有可追求的东西**（`Models.kt:293-304`） | 游戏玩家 |
| 13 | **历史页 90 条没有搜索**；逐天展开有 **60 天上限**（`Screens.kt:1962`）→ 第二个月起最早的几天会消失 | 大学生·一个月 |
| 14 | **习惯断一天就归零、不能补卡**（`Screens.kt:1232` 只传今天）；项目**已经算好**的「强度分」（`TaskRepository.kt:430-453`）从来没在任何界面出现过 | 游戏玩家 |
| 15 | **手机端缺数值面板**：我的页只有「今日进度 + 习惯坚持」（`ProfileScreen.kt:270-300`），而电脑端首页有本周/近 7 日柱状图 | 游戏玩家 |

---

## 四、我建议补的功能（按「测试者最想要」排序）

1. **导入/导出到用户能拿到的地方**（程序员、大学生都提；也是买断的门票）
2. **手机端补数值面板**（等级/积分/本周趋势）—— 游戏玩家最想要，工作量小、观感收益大
3. **提醒权限状态与补救入口**（设置里显示"通知被关了，点这里去开"）
4. **历史页搜索 + 去掉 60 天上限**
5. **习惯补卡**（断一天不归零，允许补前一天）

## 五、UI/文案类（不影响功能）

- 「每日任务刷新时间」「提前多久提醒」两个**改不了的设置**要么接通要么先撤掉 —— 摆着能改但不生效，比没有更伤信任
- 术语对中年/非技术用户偏专业（本地报告正在生成，若出结果我再补一份「看不懂的词」清单）

---

## 六、我给测试者划的诚实边界（他们自己也标了）

- 真机一个月里**到底几天没响**、桌宠实体手感、耗电发热 —— **没测到**（无法真机跑一个月）
- 桌宠/挂件的实际动效、多设备真机联调 —— 未覆盖
- 所有涉及"点了会发生什么"的结论，我这次已逐条复核**代码路径**；但**未逐条真机复现**，第 3~7 条建议动之前先写复现步骤

---

# 第二轮：24 份代入式测试（2026-09-22）

boss：「至少 5 次测试 越多越好…要分男女 分成年人和未成年 分工作性质…再测！」

## 规模
第一轮 4 份 → **本轮 24 份**（连同前 4 份，共 28 人次）。每份都跑**一个月时间线**
（第 1 天 / 第 3 天 / 第 1 周末 / 第 2 周 / 第 3 周 / 第 1 月末），要求标注
【已核实】+文件:行号 或 【推测】+依据，**不许编造复现步骤**。

覆盖面：未成年 3 份（14 男 / 17 男 / 16 女）｜女性 11 份｜体力劳动 3 份｜
退休与独居 2 份｜能力受限 2 份（ADHD / 色弱）｜技术 2 份｜服务与教育 4 份｜家庭与个体 2 份。

## 总量
**356 条问题行**，其中「崩溃 / 数据错 / 功能不可用」级 **177 条**（崩溃 14 / 数据错 52 / 功能不可用 111）。

## 第一轮的最高频结论在本轮被反复印证
| 簇 | 命中报告数 | 一句话 |
| --- | --- | --- |
| 提醒链路 | 79 条 | 新建/编辑任务**不排提醒**（排程只在冷启动/开机/上次响过）；「延迟1天」实际 +1 毫秒 |
| 备份 | 24 条 | 只有导出**没有导入**；导出文件里**带着配对密钥** |
| 次数任务 | 18 条 | 点一次就归档，「次数」完全无效 |
| 双端一致 | 16 条 | 电脑端首页三个数字打架；积分绝对值同步互相覆盖 |
| 重复规则 | 12 条 | 「每周一三五 / 隔天」**选了等于没选** |
| 删除/误操作 | 12 条 | 详情页删除无二次确认 |

## 本轮已修（v5.23.0，两批，均编译通过）
1. 任务增删改后**立即重排该条提醒**（`TaskViewModel.rescheduleOne`）
2. 「延迟1天」= 真正的 24 小时（原参数单位被当成毫秒）
3. 通知按钮 `dueAt!!` 空指针崩溃 → 判空
4. 每日型任务 due_at 不再被延迟平移
5. `schedule()` 时刻已过不再静默丢弃
6. 点提醒通知**直接打开那条任务**（原来 Activity 不读 intent）
7. 补 `USE_FULL_SCREEN_INTENT` 权限（原来静默降级）
8. 详情页删除加二次确认
9. **「每周一三五 / 隔天」真正生效**（保留 habitRule + `nextOccurrenceFor()` 折算）
10. 导出备份**排除 `auth_secret`** 配对密钥
11. **次数任务改为累计推进**（满次数才归档、每次给分、总额不变）
12. 次数任务可以设提醒时间（原来永不提醒）并每天重复
13. 修夜间「白字压米白底」1.2:1 不可读

## 未修（8 项，见 `requirements/项目需求-v5.23.0.md` 第三节）
批量恢复扣分可刷分｜没有导入｜双端覆盖与积分同步语义｜电脑端提醒与数字口径｜
「提醒方式/范围」死设置｜分类功能死代码｜挂件「+」失效与旧名｜视觉一致性一组。

## 验证状态
两批均 BUILD SUCCESSFUL；**真机验证未做**（adb 连不上：无线调试未开）。
所有修复一律标「待真机验证」，不做「应该好了」的表述。

---

# 第三轮：35 份报告（2026-09-22 晚）

boss：「无线调试开了 你修完这些之后开始第三轮测试 之前的所有角色重新测试
（分为接着用和重新用）另外增加更多可能使用的」

## 一、先补上 v5.23.0 的真机验证（5/5 通过）
1 新建任务后**立即排提醒**（未冷启动，闹钟 1→2 条，新增 09-23 12:00）｜
2 次数任务可设提醒（due=09-23 12:00 / rr=daily）｜
3 次数任务累计（点一次 progress=1、未归档、积分 9→10）｜
4 每日型 due_at 不被延迟平移（09-18 09:00 原样）｜
5 详情页删除二次确认（弹出"删除后无法恢复"，取消后任务完好）｜crash.log 0 行。

## 二、第三轮三种情境（新增 11 份，累计 35 份）
- **接着用**（第 2 个月）：大学生 / 游戏玩家 / 护士 / 物流司机 / 退休教师
- **重新用**（换机重装、数据全丢）：初三男生 / 全职妈妈
- **新角色**：孕妇 / 低视力用户 / 夜班保安 / 直播主播

## 三、修复核对结论（多个代理独立去代码里核实，不采信声明）
v5.23.0 的 13 项 → **全部在代码层落地**（各报告给出 文件:行号）。
**唯一遗留 ⚠️**：「每周一三五」**提醒**已按星期正确计算，但**今日列表/日历的 SQL 不按 weekday 过滤**
（`Database.kt` observeMainListToday）→ 每周任务仍天天出现在今日待办。

## 四、第三轮新发现（严重级）
1. ⭐⭐ **「未处理自动升级」UI 已删、代码默认仍开**（`ReminderScheduler.kt:269`）→ 夜里通知自动升级成响铃，用户无法关闭
2. ⭐⭐ **无障碍缺口**：自绘 Toast / 庆祝层非 LiveRegion（读屏全静默）；批量只能长按+滑选；同步状态无语义；多处点击区 <48dp
3. ⭐⭐ **产检 / 硬 deadline 没有提前提醒**（提前量是死设置，全仓无消费点）
4. ⭐⭐ **电脑端无系统通知**（仅页内弹窗）→ 窗口被遮挡就完全看不到
5. ⭐⭐ **换机全丢**：无导入 + 授权码不随备份 + 等级/连击归零，且无"老用户"识别
6. ⭐ 连击断裂无补卡（长连击变负担）；等级约 Lv.10 撞天花板后成长感消失
7. ⭐ 历史页无搜索、日历锚今天，数据量上来后找不到东西

## 五、未修清单（8 + 6）
沿用 8 项（批量恢复刷分 / 无导入 / 双端覆盖 / 电脑端口径 / 提醒范围死设置 / 分类死代码 / 挂件 / 视觉一致性）
+ 新增 6 项（每周列表过滤 / 自动升级开关 / 无障碍三件套 / 提前提醒 / 电脑端系统通知 / 睡眠时段静音）

---

# 第四轮：平行身份 + 原身份 × 两种状态（2026-09-22 夜）

boss：「这次不用更多的身份了 而是找到之前已经有的测试者身份的**平行身份**（同学/同事/老婆老公…）
我要的不只是找出bug 而是**使用体验** 更好的使用体验 为此甚至可以**颠覆整个软件**
另外你需要去找一些**商业化的定律**之类的 比如木桶定律 比如马太效应 比如幸存者偏差」
追加：「（两类测试者）同样要有**继续使用**和**无使用记忆**的状态」

## 一、先修 4 项（v5.24.0，编译 + 真机均通过）
1. ⭐ **「提前提醒」**（全新）：Task 加 `remind_ahead_min`（Room v4→v5 迁移）+ 新建页 UI
   （到点/提前5分/30分/1小时/1天）+ 排程把响铃时刻前移
2. **「每周一三五/隔天」不再天天进今日**（`TaskRepository.shouldShowOnDay()`）
3. **「未处理自动升级」默认关闭**（开关 UI 早被删、用户无法关闭 → 夜里会自动升级成响铃）
4. **自绘 Toast 加 liveRegion**（读屏此前完全静默）

真机验证：`user_version=5`、新列存在、**数据完好**、提前提醒 UI 出现、`crash.log` 0 行。

## 二、第四轮 9 份（矩阵：平行/原 × 继续/无记忆）
| 测试者 | 类型 | 状态 |
| --- | --- | --- |
| 大学生室友 | 平行·同学 | 旁观+上手 |
| 全职妈妈丈夫 | 平行·配偶 | 继续使用 |
| 独居老人女儿 | 平行·子女 | 无使用记忆 |
| 程序员同事（前端） | 平行·同事 | 继续使用 |
| 护士同事（ICU 医生） | 平行·同事 | 继续使用 |
| 护士 | 原身份 | 无使用记忆 |
| 物流司机 | 原身份 | 继续使用（第 3 个月）|
| 游戏玩家 | 原身份 | 无使用记忆 |
| 初三男生 | 原身份 | 继续使用（第 3 个月）|

（「退休教师·无使用记忆」因用量限流未跑完，下次补）

## 三、第四轮结论：体验层的结构性短板
1. ⭐⭐ **协作根本不存在**：无负责人/指派字段、双端 LWW 静默覆盖、无活动流、提醒只响服务器那台
2. ⭐⭐ **远程照护不可行**：看不到"他今天吃药了没"，无回执
3. ⭐⭐ **隐私是专业场景死线**：锁屏通知 `VISIBILITY_PUBLIC` 明示任务名；DB 明文
4. ⭐ **信息架构过载**：两个悬浮钮、无字图标、中央键语义漂移、隐式左右滑
5. 长期（第 3 个月）：任务变僵尸、只真用 2~3 个功能、连击无补卡、两月数据无搜索
6. 首次（无记忆）：**无新手引导**，功能自己发现不了
7. 等级：设计规范 Lv.1–4 **零光效** → 用两个月没见过会动的徽章
8. 键盘效率：仅 2 处 keydown、行不可聚焦、右键被吞、无批量

## 四、商业定律应用（各报告共 68 条，提炼 8 条）
木桶（短板=协作/隐私/可迁移）｜马太（缺"被推荐"的钩子）｜幸存者偏差（没测"3 天卸载"的人）｜
峰终（终值=换机/月末很糟）｜Kano（提醒/数据/隐私是必备属性）｜损失厌恶（只会扣、没有找回的路）｜
福格 B=MAP（动机够、门槛高、提示弱）｜二八（90% 只用 2~3 个功能）

## 五、下一步
导入+授权码恢复 ｜ 首启引导+首页减法 ｜ 协作三件套 ｜ 隐私档位 ｜ 月报+补卡+等级动效
