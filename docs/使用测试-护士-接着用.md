# 使用测试 · 护士（女 26 · ICU 三班倒）· 第二个月接着用

> 前提：已用满一个月，v5.23.0 刚推。所有判定以代码为准（需求文档自身也写明「真机验证未做」）。

## 1 第二个月第一印象
升级后旧任务都还在，之前选了等于没选的「每周三教研」「隔天背单词」现在真的只在那几天冒出来（修复 #9 生效）；但一进详情页延迟、再点一条「仅电脑端」的缴费任务，手机照样响——这一轮修的全是"新建/编辑/通知按钮"那条链，应用内另一条路和"范围"还没动。

## 2 时间线
- **第 2 月第 1 天（面对历史数据）**：旧「每周三教研」只在周三进今日（#9✅）；点一条旧「仅电脑端」缴费，手机仍响（问题#2）。
- **第 1 周末（清理/归档）**：批量删 12 条过期排班，撤销条深褐底白字清晰（#13✅）；顺手发现详情页「延迟」对话框不改闹钟（问题#1）。
- **第 2 周（倒班提醒靠不靠得住）**：白班切夜班，通知上「延迟1天」这次真 +24h（#2✅），但应用内延迟对话框仍不重排（#1）；「仅电脑端」缴费照样手机响。
- **第 3 周（大夜班后睡醒一堆提醒）**：周三大夜班手机关机睡到下午，开机瞬间 6 条过期限时任务全弹（问题#5）；凌晨 1 点打卡的「背单词」被算成次日，连续天数从 21 断到 1（问题#7）。
- **第 4 周（回顾 + 买断决策）**：电脑前靠桌宠，确认桌面端没到点提醒（#9）；积分曾两端互相覆盖（#3）。
- **月底（换手机/重装）**：导出 JSON 成功且不含 auth_secret（#10✅），但目录文件管理器进不去（#10 目录问题）；重装后无导入入口（#4）→ 几十任务要重输，买断码得重填。

## 3 问题清单
| # | 问题 | 级别 | 依据 |
| --- | --- | --- | --- |
| 1 | 详情页「延迟」对话框不改闹钟：只更 DB 的 dueAt，不重排 AlarmManager/WorkManager；通知按钮才重排 → 应用内延迟后旧闹钟仍按原时间响 | 严重·功能不可用 | 【已核实】TaskViewModel.kt:293-296 vs ReminderScheduler.kt:330/369 |
| 2 | 「提醒范围/方式」是死设置（移动端）：schedule/showReminder 不读 scope，带 dueAt 即发手机通知；设「仅电脑端」仍手机响 | 严重·功能不可用 | 【已核实】ReminderScheduler.kt:121-164、190-199；rescheduleOne:136-165 |
| 3 | 双端同时改同一条任务互相覆盖（按本机时钟 LWW） | 严重·数据错 | 【已核实】TaskRepository.kt:780/790/817（需求未修#3） |
| 4 | 换机/重装无导入：仅导出无导入入口 | 严重·数据丢失 | 【已核实】SettingsScreen.kt 仅 exportJson，无 import（需求未修#2） |
| 5 | 大夜班醒来提醒堆爆：已过期非每日任务开机即全弹，无静默/免打扰 | 严重·无声失败 | 【已核实】ReminderScheduler.kt:222-228 |
| 6 | 「未受理升级」走 WorkManager，本文件注释承认可被系统无限期推迟 → 升级不响 | 严重·无声失败 | 【已核实】ReminderScheduler.kt:284-291 + 注释147-153 |
| 7 | 夜间连续天数按本地 0 点切，凌晨打卡算次日，连续天数易断且无补救 | 中·数据错 | 【已核实】TaskRepository.kt:418-438 |
| 8 | pinToHome/unpinToRepo 把 dueAt 设成 now，破坏每日型「每天几点」 | 中·数据错 | 【已核实】TaskRepository.kt:600；rescheduleOne:162 |
| 9 | 桌面端无 per-task 到点提醒（仅 22:00 未完成 tick） | 中·功能不可用 | 【已核实】lib.rs:260-299（需求未修#4） |
| 10 | 导出目录在 Android/data 下，11+ 文件管理器进不去，备份自己拿不到 | 中·数据错 | 【已核实】SettingsScreen.kt:970（需求未修#2） |
| 11 | 升级提醒每档各 enqueue 一条，可能多档同 pending 刷屏 | 中·无声失败 | 【已核实】ReminderScheduler.kt:267-291 |
| 12 | 通知/闹钟 id 用 uuid.hashCode()，两任务哈希相同会互相覆盖/顶替 | 中·数据错 | 【已核实】NotificationHelper.kt:199、ReminderScheduler.kt:184 |
| 13 | 买断授权码不在备份内，换机重装需重填，丢则降级回免费 | 轻·数据 | 【推测】导出仅 FullSyncPayload（SettingsScreen.kt:965），无 license 字段 |
| 14 | 第二个月成长感弱：等级曲线 Lv1→Lv3 需 130 分，低分任务攒得慢，徽章解锁钝 | 轻·体验 | 【已核实】Levels.kt:293-303、RewardRules.kt:354-362 |

## 4 v5.23.0 修复核对（逐条去代码）
| # | 声明 | 结果 | 代码依据 |
| --- | --- | --- | --- |
| 1 | 增删改后立即重排提醒 | ✅ | TaskViewModel.kt:136-167、179/186/192/255/207/217 |
| 2 | 延迟1天 +1ms→24h | ✅ | ReminderActionReceiver.kt:330 `24L*3600_000L` |
| 3 | 通知按钮 dueAt!! 空指针→判空 | ✅ | ReminderActionReceiver.kt:343-355 |
| 4 | 每日型 due_at 不被延迟平移 | ✅ | ReminderActionReceiver.kt:360-368（走 nextDailyOccurrence） |
| 5 | schedule() 时刻已过不静默丢弃 | ✅ | ReminderScheduler.kt:126-127 `now+1500L` |
| 6 | 点通知直接开任务 | ✅ | MainActivity.kt:128-132、144-151；NotificationHelper.kt:145-152 |
| 7 | 补 USE_FULL_SCREEN_INTENT | ✅ | AndroidManifest.xml:23 |
| 8 | 详情页删除二次确认 | ✅ | TaskDetailScreen.kt:82-98 |
| 9 | 每周一三五/隔天 nextOccurrenceFor 生效 | ✅ | ReminderScheduler.kt:81-118；消费点 :221、:162-164；保存保留 habitRule AddEditTaskScreen.kt:795-800 |
| 10 | 导出排除 auth_secret | ✅ | SettingsScreen.kt:965-967 `filterNot{key=="auth_secret"}` |
| 11 | 次数任务改累计推进 | ✅ | TaskRepository.kt:336-353；桌面 lib.rs:775-803 一致 |
| 12 | 次数任务可设提醒 | ✅ | AddEditTaskScreen.kt:649-652、788-791、797-799 |
| 13 | 夜间米白底+白字→固定深褐 | ✅ | Screens.kt:1713-1716 `0xFF3B2A20`；UiFoundation.kt:713 |

> 13 条全部「编译通过」已核实，但需求文档明确**真机未验证**；且本轮未覆盖「应用内延迟对话框」「提醒范围」「双端冲突」「无导入」「桌面无提醒」等残留（见问题#1/#2/#3/#4/#9）。

## 5 会不会继续用 / 买断
**会继续用**（提醒主链已明显变可靠，每日/周期任务这轮是真的修好了），但**暂不买断**。理由：① 免费追踪上限 1 个够我盯当天最重要的事；② 换机/重装无导入（#4）+ 授权码不进备份（#13）是硬伤，买断后一旦换手机反而更麻烦；③ 桌面端没到点提醒（#9），我电脑前干活靠它不靠谱，买断的"完整版"权益在这块没体现。等导入功能补上、双端冲突解决，再考虑 ¥5~12 买断。
