# 使用测试 · 众包外卖骑手（25 岁 · 一个月）

> 被测：**next任务**（手机端为主，电脑基本不用）。测试者：25 岁男，众包骑手跑两个平台，一天 10 小时在电动车上，手机挂支架、日晒雨淋、**全程戴手套**，等取餐只有 3 分钟。要记：跑单目标、换电、保险续费、家里的事、每周打钱。
> 手段：**只看截图与源码，没真机点一个月**。【已核实】=代码/截图确证+行号；【推测】=给依据；看不出写「看不到」。

### 1 第一印象（≤3 句）

字大、就右下角一个红加号，能上手；可我要记的跑单量、换电、保险，四类里一类都对不上。顶栏一堆数字（9 分、Lv、距下一级），我想问的却一字没有：**到点到底响不响**。

### 2 一个月时间线

| 时间 | 做了什么 | 结果 / 问题（依据） |
| --- | --- | --- |
| 第 1 天 | 记"12:40 换电" | 类型默认「每日任务」(`:465`)→一存就天天重复。记一条要 **7 步** |
| 第 3 天 | 跑来跑去忘了 | **一声没响**：提醒时间被清成 null(`:762`)，勾过的响铃零提示(#2) |
| 第 1 周末 | 设"每天跑够 30 单" | 每日只能**每天打一次卡**；次数是累计、不归零(`TaskRepository.kt:306-322,119`)(#3) |
| 第 2 周（雨·手套） | 支架上打勾 | 长按 320ms 跳编辑，手套一压就中；完成键 22dp 无确认(`Screens.kt:513-530,780`) |
| 第 3 周（摔机换机） | 装回 App | 任务搬不回，**连买断授权也丢**：导出在我翻不到的 `Android/data`，没电脑(#6) |
| 第 1 月末 | 回看这月跑了多少 | "我的"只有"今日进度 N 个待完成"+"习惯 N 天"，**没有月汇总**(`ProfileScreen.kt:270-310`) |

### 3 问题清单

> 12 条：数据错 3、不可用 5、体验/文案 4。

| # | 严重度 | 问题 | 依据 | 建议 |
| --- | --- | --- | --- | --- |
| 1 | 高·数据错 | **次数任务的"次数"是假的**：点一次就归档，不算不显示进度 | `TaskDetailScreen.kt:600,469`、`TaskRepository.kt:323,334-337`、`Screens.kt:740` | 满次数才归档 |
| 2 | 高·静默 | 次数任务**设不了提醒**：截止被清空，照样能勾"振动/响铃"，永不排程、零提示 | `TaskDetailScreen.kt:762,691-712`、`ReminderScheduler.kt:156` | 整块隐藏 |
| 3 | 高·不可用 | **"每天跑够 N 单"表达不了**：每日只能打一次卡，次数是累计、不按天重置 | `TaskRepository.kt:306-322,119` | 加"每日 N 次" |
| 4 | 高·不可用 | **没有语音/快捷方式**；挂件只显示"追踪中"任务，其「+」是**死的** | `TrackWidgetProvider.kt:26-35` vs `MainActivity.kt`（无 `getStringExtra`）、`widget_track.xml:59` | 挂件真·新建 |
| 5 | 高·不可用 | **间隔提醒表达不了**：换电想"每 2 小时"，每日任务只有"每天几点" | `TaskDetailScreen.kt:1012-1042` | 加"每隔 N 小时" |
| 6 | 高·数据错 | **换机连买断一起丢**：码只存本机，备份/搬家全排除，只给订单号**不给码** | `License.kt:41-43`、`data_extraction_rules.xml`、`SettingsScreen.kt:1071-1074` | 码可复制 |
| 7 | 高·不可用 | **「不提醒」是假开关**：选了照样排、照样响 | `Models.kt:103`、`ReminderScheduler.kt:142-172` | 先判 scope |
| 8 | 中·数据错 | 设置里**已删掉**的"未处理自动升级"仍在跑，走作者注释里"15 小时不执行"的机制 | `ReminderScheduler.kt:217-239,96-104` vs `SettingsScreen.kt:416-418` | 停掉或接回 |
| 9 | 中·手套 | **320ms 长按进编辑**，手套一压就中；完成键 22dp、无确认 | `Screens.kt:513-530,769,780` | 阈值调长 |
| 10 | 中·骑行 | 到点**只响一次**，错过只剩一条通知；提示音走**通知音量**，蓝牙导航会盖掉 | `NotificationHelper.kt:44-53,49-51`；真机**看不到** | 响到我处理 |
| 11 | 中·体验 | 完成一条盖**整屏 2.4 秒**；"我的"**没有周/月汇总** | `UiFoundation.kt:742`、`ProfileScreen.kt:270-310` | 轻提示+汇总 |
| 12 | 中·文案 | 同一件事两套名："每日/目标/限时/**次数**"↔行上"习惯/目标/重复/**单次**"；不懂：里程碑、速记、仓库、追踪中、阅览方式、JSON、保活、白名单/自启动、同步服务器、授权码、双端。名字还有三个：next任务／前台通知「任务栏」／挂件「任务指南 · 追踪中」 | `TaskDetailScreen.kt:599-600` vs `UiFoundation.kt:631-639,680-681`；`SyncService.kt:120`、`TrackWidgetProvider.kt:49` | 统一一套词 |

### 4 我最想要的 3 个功能

1. **一句话快录**：锁屏/挂件按住说"换电 12:40"就存下、到点响——现在挂件「+」是死的。
2. **提醒靠得住**：响到我处理；能设"每隔 2 小时"和"提前 3 天"。
3. **"每天跑够 N 单"**：每天 N 次、次日归零；换机能搬走。

### 5 会不会买断，为什么

**不买。** 完整版卖"同时追踪多个任务／导出备份／外观主题"(`SettingsScreen.kt:1081`)，我不追踪、没电脑、不换主题，**一条都用不上**，真正被锁的只有追踪上限(`License.kt:126-127`)；我想要的（快录、响到我处理、跑单计数、换机能搬）都做不到。现在买只是支持作者。把第 2、3 条和"快录"做出来，¥12 我掏得心甘情愿。
