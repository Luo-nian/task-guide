# 使用测试 · 军校学员｜第一个月（21 岁 / 大二 / 6:00 起 22:30 熄灯 / 白天手机集中保管 / 电脑只在机房）

> 只看截图与代码，没真机点过。推测标【推测】，代码能证的标【已核实】+文件:行号，看不出的写"看不到"。

## 1 第一印象

米白+赤陶，卡片干净，不花哨，合我意。但「我的」页一张金色渐变等级卡顶着"历练学徒 / 距下一级 41 / 18%"和"敢开始，就已经赢了一半"——队列里不这么说话。首次打开无引导，也没告诉我提醒到底会不会响。

## 2 一个月时间线

**第 1 天**（周末外出装的）建「体能」每日任务，点「每周」勾一三五、提醒 6:30。第 2 天发现周二它也在、还提醒（问题 2）。

**第 3 天**（手机被收两天）提醒全落空，没有补给我；拿回手机解锁那刻，逾期提醒一条条往外弹（问题 11）。**第 1 周末**想把整周训练表一次建好，没有复制/模板，只能一条条敲（问题 10）。

**第 2 周** 集体活动打断打卡，连续天数归零，翻遍界面没有补卡（问题 7）。想记 3 公里用时和引体个数，只找到"备注"和"次数"（问题 8）；次数任务点一次就归档，没看见 3/8（问题 1）。

**第 3 周**（考核周）改了几条任务时间，点通知上的「延迟」崩了一次（问题 9）。机房电脑改的任务，回来看手机没有（问题 12）。

**第 1 月末** 翻历史：日历上二四六全是"未完成"，20 多条不是我漏的（问题 7）。导出备份想留一份，发现导不回来（问题 5）。

## 3 问题清单

| # | 严重度 | 问题 | 依据 | 建议 |
| --- | --- | --- | --- | --- |
| 1 | 数据错 | 手机端「次数任务」点一次即归档，次数不累计（电脑端会累计） | TaskDetailScreen.kt:469-474 once→ONCE；TaskRepository.kt:323 只累加 MILESTONE；lib.rs:775 电脑端累加 | 按 count>1 累加，满额才结算 |
| 2 | 功能不可用 | **「每周一三五 6:30」存了没人读**：天天出现、天天提醒、天天判未完成 | TaskDetailScreen.kt:1264-1282 存 rule；全仓仅 ReminderScheduler.kt:42-46 认 daily | 排程与列表都按 repeat_rule 过滤 |
| 3 | 功能不可用 | 「不提醒」「仅电脑」在手机端无效，照样响 | TaskDetailScreen.kt:767 存 null → ReminderScheduler.kt:158 回落全局默认；:221 scope 未用 | 按 scope 拦截通知 |
| 4 | 功能不可用 | "未受理自动升级"UI 已删、逻辑仍默认开：选「振动」5 分钟后升提示音→响铃 | SettingsScreen.kt:100/416；ReminderScheduler.kt:218 默认 true | 删升级链或给开关 |
| 5 | 功能不可用 | 备份只能导出、不能导入，换机/重装取不回 | SettingsScreen.kt:958-971；全仓无导入；Manifest allowBackup=false | 加「从备份导入」 |
| 6 | 功能不可用 | 授权码存 prefs、不在备份里 → 换机后完整版失效 | License.kt:37-38；TaskRepository.kt:724-737 只带 DB settings | 授权码随备份保存 |
| 7 | 数据错 | 一三五的课表在二四六被判"未完成"：日历一个月 20+ 条假漏卡，连续天数天天打回 1 | Screens.kt:1986-1990 逐天生成；TaskRepository.kt:398-418 只认自然日 | 按计划日结算 |
| 8 | 功能不可用 | 3 公里用时、引体个数无处记（无按天数值字段） | Models.kt:463-469 HabitLog 只有 check_date；desc 是静态备注 | 打卡时填数值 |
| 9 | 崩溃 | 通知「延迟」键：任务改成无截止后 `dueAt!!` 空指针【推测】 | ReminderScheduler.kt:283、296 | 判空改为撤销提醒 |
| 10 | 难用/困惑 | 建整周表没有复制/模板/批量新建，只能一条条敲 | Screens.kt 多选仅删除/恢复；详情页只有步骤 JSON | 加「复制任务/周计划模板」 |
| 11 | 难用/困惑 | 手机被收：错过的时刻不补，拿回时逾期提醒一次性全弹 | ReminderScheduler.kt:173-177；TaskBarApp.kt:46 冷启动跑 | 汇总成一条「错过的 N 项」 |
| 12 | 无声失败 | 机房电脑改了任务、手机不在 → 推送失败只写日志，界面看不出 | sync.rs:571-620 仅 `log::warn` | 未同步项加角标+重试队列 |
| 13 | 文字/一致性 | 挂件标题还是旧名「任务指南」；等级名"历练学徒→寰宇传说"偏玄幻 | TrackWidgetProvider.kt:49；文案排查文档:290 | 统一为 next任务；等级名收一档 |

## 4 我最想要的 3 个功能

1. **真正按计划日的周表**：一三五 6:30 只在计划日出、只在计划日算连续，二四六不出现也不扣我。
2. **按天记数值 + 月末一张训练成绩表**（3 公里用时、引体个数、趋势）。
3. **一次请假/补卡豁免**：集体活动打断的连续天数能补回一次——断了就再也不想开。

## 5 会不会买断，为什么

**现在不买；修好第 2、7 条就买（≤12 元直接付）。** 我对"分钟级准"和"连续天数"最认真，这两样现在是坏的——买到的会是一个不准的闹钟和一个会冤枉我的日历，付钱反而更来气。付费点（多追踪几个任务）对我不是痛点，值钱的是"替我把作息守住"。且买断要跳爱发电、等作者人工发码粘贴，手机还不一定在身上，这一步就会劝退我。不注册、数据不过云、个人开发者这三条我愿意支持——先把周表和连续天数做靠。
