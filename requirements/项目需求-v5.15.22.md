# v5.15.22 需求清单（boss 2026-09-13 15:21 提出，14 项）

> 状态：⬜ 待做 ｜ 🔄 进行中 ｜ ✅ 已完成（真机/CDP 验证过）｜ 🚧 受阻
> **2026-09-13 收尾：14 项全部完成。** 会话内追加：M5b 涟漪改双层（内圈常驻+外圈扩散）、
> D4 近 7 日完成热力条、D5 双端排版层级（标题衬线/数值放大/分区加重）。
> **M11 真正根因 = 系统代理(127.0.0.1:7892)劫持 reqwest 的 HTTP**，详见 `debug/debug-v5.15.22.md`。

boss 原话（附 3 张截图）：
> 手机端 应该是默认日历形式查看历史任务；而且日历的日期对应任务数量有点小而且位置偏下；
> 而且为什么12 13日没有任务数量 历史任务不是已完成任务 已逾期未完成的每日任务也算进去；
> 我说我希望看到页面切换的UI；追踪任务后 取消追踪键应该有涟漪UI；
> 你还是没有以列对齐追踪键和完成键；设置优先级高中低的时候 点到那三个按键"高""中""低"的时候 颜色应该有所区分；
> 编辑任务页面 选择每日任务的习惯周期的时候 默认是每天没问题 点到隔天的显示也没问题 但是在从每天切换到隔天 再去点每周按键 就会切换成每天 只有从每天直接切换去每周才能正常显示；
> 日历形式下 点击任务依旧能看到任务详情 这个功能没问题 但是要只能看到任务详情 不能编辑 不能完成 没有任何功能按键 否则就可以通过日历进行刷分了；
> 为什么学英语10分钟 和隔天喝水8杯两个任务完成不了？点了完成就只弹出积分获得 但是任务还挂着；
> 双端的任务还是没能完全同步；
> 电脑端 太扁了这一栏；追踪按键的图标应该更新了 和追踪页面侧边栏按键的图标一样；
> 还有这里的 电脑端也加点UI啊 如果你是为了减少cpu占用那当我没说

---

## A · 手机端（M）

| # | 需求 | 状态 | 落点 / 验证 |
| --- | --- | --- | --- |
| M1 | 历史任务**默认日历形式** | ✅ | `HistoryScreen.calView` 初值 true；真机截图确认默认进日历 |
| M2 | 日历日期角标**太小 + 位置偏下** | ✅ | `TaskCalendar.kt` 角标右上角 TopEnd、10.5sp、底色 0.72；真机确认 |
| M3 | 历史任务数据源把**已逾期未完成的每日任务**算进来 | ✅ | `observeArchiveWithOverdue`（已完成∪逾期未完成）+ `expandHistoryByDay` 每日任务按天展开（漏打卡=未完成）；真机确认 9/1–9/13 每天有数量 |
| M4 | 页面切换 UI（左右滑动） | ✅ | 主轴三页(主页/追踪/我的)方向感知滑动切换 + 220ms 滑动转场 + 手势滑动切页；真机左滑确认 |
| M5 | 取消追踪键要有**涟漪 UI** | ✅ | `TrackRippleKey`：内圈常驻呼吸环（15dp，boss 要求"靠内圈加一层"）+ 外圈 16→27dp 扩散环，1.9s 循环；四个取消追踪键全替换 |
| M6 | 追踪键与完成键**列对齐** | ✅ | 根因：TaskRow 内边距 end 6dp vs HabitRow 12dp + 无键间距。统一 end 12dp + 键间距 10dp + 图标 22dp；真机确认跨行同列 |
| M7 | 优先级 高/中/低 按键**颜色区分** | ✅ | `PriorityFilterChip`：高=朱砂/中=深赤陶/低=灰，选中实底白字；真机确认 |
| M8 | 习惯周期 bug：每天→隔天→每周 会重置为每天 | ✅ | 根因：点「每周」时 `selectedDays` 为空 → `serializeWeekly(空)=null` → remember key 变化重建 state。修复：点每周且空集时默认填 `{1,3,5}`；编译通过，交互待 boss 实测 |
| M9 | 日历里点任务**只读**（无编辑/完成/功能键） | ✅ | 新路由 `ro/{uuid}` + `TaskDetailScreen(readOnly)`：操作区/添加步骤/步骤打勾全不渲染，标题标「只读」；真机确认 |
| M10 | 🐛 「学英语10分钟」「隔天喝水8杯」完成不了 | ✅ | 上一轮已根治（repo 先 normalize 再判状态 + 弹窗改按积分增量）；本轮编译+装机通过 |
| M11 | 🐛 双端任务未能完全同步 | ✅ | **双层根因**：①系统代理劫持 reqwest HTTP（502/解码失败）→ 全部改 `lan_client()` 直连；②桌面→手机无全量推送 → `push_full_to_mobile` 216 条/批 120 + 3 轮重试。实测双端任务 59/59、步骤 48/48、打卡 75/75，差异 0 |

## B · 电脑端（D）

| # | 需求 | 状态 | 落点 / 验证 |
| --- | --- | --- | --- |
| D1 | 等级栏**还是太扁**（图1） | ✅ | 真凶：文件尾部 `.level-card{padding:10px 14px !important;min-height:56px}` 覆盖了上轮的加厚。改为 padding 20/22、min-height 112、图标 68px；CDP 确认 computed style 生效 |
| D2 | 「追踪任务」键图标换成与追踪页/侧边栏一致（图2） | ✅ | 详情页 `.dva-b.track` play→goal；CDP 校验嵌入代码已生效 |
| D3 | 「追踪中」统计卡右上角图标也要换（图3） | ✅ | index.html `data-icon="crosshair"→"goal"`；CDP 确认 |
| D4 | 电脑端加 UI（自主发挥，静态为主） | ✅ | 新增「近 7 日完成」热力条（纯静态，归档 done_at 按天计数）；CDP 确认 7 列渲染 |
| D5 | （追加）双端排版/字体/层级清晰化 | ✅ | 电脑端：标题衬线(Noto Serif SC)、统计数值 24px/800、分区标题加重、近7日条字放大；手机端：页面标题衬线 21sp、分区标题 13sp Bold、任务名 16sp SemiBold |

## 排查顺序（已完成）

1. **M10** ✅ → 2. **M3** ✅ → 3. **M11** ✅（代理根因）→ 4. M8 ✅ → 5. M1/M2/M9 ✅ → 6. M5/M6/M7 ✅ → 7. M4 ✅ → 8. D1-D5 ✅
