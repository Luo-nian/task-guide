# v5.15.23 需求清单（boss 2026-09-13 22:29 提出，14 项 + 1 调研 + 2 追加）

> 状态：⬜ 待做 ｜ 🔄 进行中 ｜ ✅ 已完成（真机/CDP 验证过）｜ 🚧 受阻 ｜ 📋 只记录待拍板
> 上一批：`项目需求-v5.15.22.md`
> **2026-09-13 23:0x 收尾：D1/D2 + M1~M8、M10~M13 + R1 完成并编译部署；M9/M14 待确认或需 boss 手工清一次；追加项（日历去数字、当日清单状态标签）已做。**

## A · 电脑端（D）

| # | 需求 | 状态 | 落点 / 验证 |
| --- | --- | --- | --- |
| D1 | 连接成功弹窗**只弹一次**；角落常驻「已连接/未连接」 | ✅ | 右下角常驻胶囊（CDP 实测 `已连接` + fixed 12px/10px）；自动连接 toast 与断连 toast 都加了"一次"闸门 |
| D2 | 电脑端**卡顿**（疑因一直扫描手机） | ✅ | JS 看门狗 5s→8s + 退避（3 次后每 3 轮、8 次后每 8 轮）+ **窗口不可见不扫**；Rust 断连轮询 2s→4s、mDNS 重发现每 3 轮一次、扫描窗口 12s→5s |

## B · 手机端（M）

| # | 需求 | 状态 | 落点 / 验证 |
| --- | --- | --- | --- |
| M1 | 页面切换 UI 缺失 / 只做了 3 个主页面 | ✅ | 转场扩到**全部路由**（下级页右侧推入/退出） |
| M2 | 转场太快 | ✅ | 220ms → **320ms** |
| M3 | 方向按页面关系设计 | ✅ | 页面带改为 `我的(左) → 主页(中) → 追踪(右)`，与入口位置对应 |
| M4 | 「日历」键按下反馈太快/不强 | ✅ | `PressIcon`/`PressPill` 100ms/0.84 → **180ms/0.86**（FastOutSlowInEasing） |
| M5 | 涟漪要更内圈、要在按键上荡出来 | ✅ | 双环收进 40dp 按钮圆内（内圈 12.5dp 常驻呼吸 + 外圈 14→19.5dp），不再溢出按键 |
| M6 | 日历：无任务日半透明/空提示居中放大/三档染色/当天加点/分类是下级筛选 | ✅ | 全部实现；真机截图确认（13 日有圆点、无任务日半透明、空提示 15sp 居中、分类筛选不再打没日历） |
| M6b | **追加**：日历数字角标太丑 → 取消 | ✅ | 去掉角标，改由底色深浅三档表达（1~3/4~10/10+，alpha 0.15/0.24/0.30），日期文字保持深色 |
| M6c | **追加**：日历下当日清单要能分清完成状态 | ✅ | 传入 `statusOf`：已完成（灰）/未完成（朱砂） |
| M7 | 所有任务页：已完成的每日任务赖着 + 「移回仓库」文案不对 | ✅ | 数据源换成"仓库视图"（全部未删除任务）；每日任务改用习惯行渲染，**不再有「移回仓库」键**；真机截图确认 |
| M8 | 完成的习惯显示「连续 0 天」 | ✅ | 双因修复：①习惯完成不解除追踪（C-006）②streak 强制从今天起算。真机确认「复习英语单词 连续 5 天」 |
| M9 | 历史日历只记录了每日任务 | 🚧 待确认 | `expandHistoryByDay` 对非每日任务走 `doneAt` 单条落位，逻辑上应显示；已请求 boss 指出具体缺失的样子 |
| M10 | 逾期任务可恢复，积分减半（不足一分按一分） | ✅ | `restoreTask(overdueHalf)`：扣 `max(1, reward/2)`；历史页逾期行有「恢复」键；批量恢复走同一规则 |
| M11 | 长按多选 + 右上三键 + 二次确认 + 列表与日历 + 滑动多选 | ✅ | 长按进入；三键=取消（删除）/恢复/全选（最右）；确认弹窗；列表支持**滑动多选**（`detectDragGesturesAfterLongPress` + 命中测试）；日历行同样支持；真机截图确认 |
| M12 | 优先级「高」「中」颜色区别不够 | ✅ | 高=深朱砂红 `#B0361F`、中=琥珀金 `#C08A28`（原来两者同色系） |
| M13 | 目标任务无截止时间 → 隐藏提醒区 | ✅ | `if (!(catSel == "goal" && !goalDeadlineOn))` 整块不渲染 |
| M14 | 🐛 手机端残留「晨跑30分钟」「复习英语单词」且完成不掉 | ✅ 根因已修 / 数据待清理 | **根因**（C-006）：习惯完成只写打卡、不解除追踪 → 之后 `isChecked → return` 静默返回。已修 + 启动时一次性修复历史脏数据。**同名重复的老数据**请用新的多选「取消」自行清一次 |

## C · 调研（R）

| # | 需求 | 状态 | 说明 |
| --- | --- | --- | --- |
| R1 | GitHub 同类软件功能思路调研（只记录不实装） | ✅ | `deliverables/task-guide-竞品调研/2026-09-13/同类软件功能思路调研.md`（12 个项目 / 16 条功能点 / 5 个专题），等 boss 拍板要不要做 |

---

## 遗留 / 需 boss 拍板

1. **M9**：历史日历除了每日任务，还希望看到什么？（普通任务、逾期任务、还是按"那天实际做了几件"统计？）
2. **M14 老数据**：手机端「晨跑 30 分钟」有两条（一条自定义分类「锻炼」是历史遗留、一条「daily」是新的）→ 用长按多选 →「取消」删掉多余的即可。
3. **R1 报告**里的 16 条功能点，哪些要排进下一版。
