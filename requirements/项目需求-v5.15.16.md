# v5.15.16 需求清单（boss 2026-09-10 22:13 提出）

> 约定：boss 每次提需求都落成本文件（带版本号），我改前先读、改完回填状态。
> 状态：⬜ 待做 ｜ 🔄 进行中 ｜ ✅ 已完成（真机/CDP 验证过）｜ 🚧 受阻

## 手机端（13 项）

| # | 需求 | 状态 | 说明 / 落点 |
| --- | --- | --- | --- |
| M1 | 延迟卡片：点「七天」直接就延迟了 → 必须点**确定**才生效 | ✅ | 预设改 FilterChip 选中态，`onConfirm(preset ?: 自定义)` |
| M2 | 「所有任务」页面也要有分类筛选 + **右上角搜索** | ✅ | AllTasksScreen 加 CategoryFilterRow + 右上角搜索键（点开 OutlinedTextField，按标题模糊匹配） |
| M3 | 任务详情页左上角加**返回键**（不依赖系统返回；系统返回保留） | ✅ | TaskDetailScreen 顶栏 Row + ic_back（系统返回未动） |
| M4 | 主页 ↔ 追踪页切换有点慢 → 快一点 | ✅ | TrackScreen 改订阅一次聚合 stepsByUuid（原来每张卡各起 Flow+查库） |
| M5 | 主页：每日任务不再单独打卡区，归入今日任务；今日任务内按分类分组；顶部筛选 = 电脑端侧边栏 | ✅ | 去掉 habit 独立区；groupedTodo 按 4 类分组；habit 用 HabitRow 混排 |
| M6 | 积分获得弹窗太丑 → 改好看 | ✅ | CompletionCelebration 重做：金色圆环光晕（呼吸动画）+「任务完成」小字 + 米白积分卡（大号 +10 积分）+ 升级横幅 + 「点击任意位置关闭」；真机连拍截图确认观感 |
| M7 | 点追踪键**不当场置顶**（只放特效），下次进主页才置顶 | ✅ | showPinSection 机制保留并验证（onJustTracked → false，回主页 LaunchedEffect 复位） |
| M8 | 追踪键与取消追踪键要有**颜色区别** | ✅ | 追踪 = Azure 天蓝 ic_track；取消 = InkMute 灰蓝 ic_close |
| M9 | 推进键在**步骤只剩一步**时变成「完成」键 | ✅ | remainingSteps <= 1 → 直接渲染完成键（两处 TaskRow 分支） |
| M10 | 追踪页三个任务要有**边框 + 间距**隔开 | ✅ | 卡片边框 1.5dp 天蓝 + 左侧色条；LazyColumn 间距 8→14dp |
| M11 | 追踪页也要有「取消追踪」「推进」两个键 | ✅ | TrackTaskCard 底部 Row：取消追踪（灰蓝）+ 推进/完成 |
| M12 | 长按编辑任务右上角加**删除**（红垃圾桶）+ 二次确认 | ✅ | AddEditTaskScreen actions + ic_delete（红色）+ TGConfirmDialog |
| M13 | 主页下滑能看完今日任务 | ✅ | 由 M5 重构 + contentPadding 150dp 保证滚动可达底部 |

## 电脑端（9 项）

| # | 需求 | 状态 | 说明 / 落点 |
| --- | --- | --- | --- |
| D1 | 个人信息里的等级徽章"扁扁的"，那一行加长一点 | ✅ | .pc-level-icon 固定 48×48 + aspect-ratio；.pc-head min-height 62px + 下边框 |
| D2 | 任务行三个键点「完成」**不弹获得积分** | ✅ | 原 celebrateCompletion 不存在（typeof 静默跳过）→ 改 showBless，按 already_done/partial/final_exp 分情况 |
| D3 | 白闪问题还在（时间缩短为一瞬） | ✅ | 根因：透明窗 + .widget backdrop-filter:blur(18px) → 合成层重建清成白。干掉根节点/弹窗层毛玻璃 |
| D4 | **今日待办页面不能上下滑动** ← P0 | ✅ | .list-view 自身 overflow-y:auto（扁平结构没有 .lv-body）+ 子项 flex-shrink:0 + 标题吸顶 |
| D5 | 逾期边框特效被**追踪特效覆盖**（追踪优先） | ✅ | .tracking 规则置于 .overdue 之后 + !important；逾期 chip 在追踪态转天蓝 |
| D6 | 任务详情右上角（编辑键旁）加**删除**键（红垃圾桶） | ✅ | .dv-del（right:50px，朱砂色）+ deleteTaskWithConfirm（confirm 二次确认） |
| D7 | 历史任务：右键无「恢复任务」；左键却显示历史详情 | ✅ | contextmenu 兼容 .lg-row；左键 onclick 移除（改 title 提示右键） |
| D8 | 添加 JSON 卡片：输入框示例改 **placeholder** | ✅ | 不再预填；textarea placeholder + ::placeholder 灰字样式 |
| D9 | 电脑端追踪任务也要**置顶** | ✅ | renderTodayView 各分组内排序 + renderListView 整表排序（追踪优先） |

---

## 验证记录

- D4 实测（改前）：`#listView` clientHeight 674 / scrollHeight 1027 / overflow visible → 确认滚不动
- D4 实测（改后）：`{"display":"flex","overflowY":"auto","clientH":674,"scrollH":1029,"canScroll":true}`，滚动 `scrollTop 0→305`（max 305）
- D1 实测：`.pc-level-icon` 48×48 正方形（aspect-ratio 1/1、flex-basis 48px），`.pc-head` 高 65px + 下边框 → 徽章不再压扁
- D3 实测：`.widget` backdrop-filter = none，窗口底色 rgb(240,233,219) 米色（不再透白）
- D6 实测：删除键 right:50px、朱砂色 rgb(188,74,52)、与编辑键无重叠、二次确认已接
- D7 实测：42 条 `.lg-row` 全部无 onclick、有 data-uuid，右键菜单弹出 [恢复任务 / 取消]
- D2 实测：行内完成 → 弹出 `{title:"+ 5 经验", msg:"任务已完成"}`
- M6 实测：真机连拍 10 帧，「交水电费」完成 → 金色圆环徽记 +「任务完成」+「+10 积分」米白卡
- M2 实测：搜索框输入 "68" 精确筛出「两个月减到 68kg」
- 编译：手机 APK `BUILD SUCCESSFUL in 1m 7s`；桌面 exe release 构建成功
