# 项目需求 v5.23.1（2026-09-22 晚）

> boss：「无线调试开了 你修完这些之后开始第三轮测试 之前的所有角色重新测试（分为接着用和重新用）
> 另外增加更多可能使用的」

## 一、本轮先做完了 v5.23.0 的**真机验证**（5/5 通过）

| # | 验证项 | 实测证据 |
| --- | --- | --- |
| 1 | **新建任务后立即排提醒**（原来只在冷启动/开机/上次闹钟响过才排） | 保存后**未冷启动**，`dumpsys alarm` 立刻从 1 条变 2 条：新增 `09-23 12:00:00` ✅ |
| 2 | **次数任务可设提醒时间** | 库中该任务 `cat=once / rr=daily / due=09-23 12:00`；详情页显示「提醒 2026-09-23 12:00」✅ |
| 3 | **次数任务改为累计推进** | 点一次「完成」→ `progress=1 / track=pending / done=0`（**未归档**）；总积分 9→10（单次份额）✅ |
| 4 | 每日型任务 due_at **不被"延迟"平移** | 广播 `ACTION_DELAY` 后 `due_at` 仍为 `09-18 09:00:00.000`、`delayed_count=0` ✅ |
| 5 | **详情页删除二次确认** | 点删除后弹出「删除任务 / 删除后无法恢复，确定吗？ / 取消 / 删除」；点取消后「健身」完好 ✅ |
| 6 | 无崩溃 | `crash.log` **0 行** ✅ |

⚠️ 未实测：非每日型任务的「延迟1天 = +24 小时」、以及「每周三」的实际排程（UI 建周期任务步骤多，留待下次）。

## 二、第三轮测试：35 份报告（本轮新增 11 份）

**三种情境**（boss 指定）：
| 情境 | 含义 | 本轮报告 |
| --- | --- | --- |
| **接着用** | 已用满一个月、有数据、习惯已建立，进入**第 2 个月** | 大学生、游戏玩家、护士、物流司机、退休教师（5 份） |
| **重新用** | 换机 / 卸载重装 → **数据全丢**，从零再来 | 初三男生、全职妈妈（2 份） |
| **新角色** | 首次使用的新人群 | 孕妇、低视力用户、夜班保安、直播主播（4 份） |

### ⭐ 修复核对结论（多个代理**独立**去代码里逐条核实，不是采信声明）
v5.23.0 的 13 项修复 → **全部在代码层落地**（各报告均给出 `文件:行号`，如
`TaskViewModel.kt:136-167`、`ReminderActionReceiver.kt:330/351-355/360-368`、
`ReminderScheduler.kt:81-127`、`MainActivity.kt:128`、`AndroidManifest.xml:23`、
`TaskDetailScreen.kt:78-98/636-652/788-800`、`TaskRepository.kt:336-353`、
`SettingsScreen.kt:965-966`、`UiFoundation.kt:714`、`Screens.kt:1716`）。

**唯一遗留（⚠️）**：「每周一三五 / 隔天」——
**提醒已按星期正确计算**（`nextOccurrenceFor` 只作用于排程），
但「今日列表 / 日历」的 SQL（`Database.kt` 的 `observeMainListToday`）**不按 weekday 过滤**，
所以每周三的任务**每天仍出现在今日待办**里。→ 已列入下版必修（属"改动语义"类）。

## 三、第三轮新发现（11 份报告的新问题）

| 严重度 | 问题 | 依据 | 来自 |
| --- | --- | --- | --- |
| ⭐⭐ 功能不可用 | **「未处理自动升级」开关已从设置页删除，但代码默认仍开启**（`reminder_escalate_enabled` 默认 `true`）→ 夜里没处理的通知会**自动升级成振动/响铃**，且用户**无法关闭** | `ReminderScheduler.kt:269` vs `SettingsScreen.kt`（开关已删） | 夜班保安 |
| ⭐⭐ 功能不可用 | **无障碍缺口**：自绘 Toast 与完成庆祝层**不是无障碍 LiveRegion** → TalkBack 完全不播报（完成/积分/入库/上限提示全静默）；批量操作**只能长按+滑选**，读屏用户进不去；同步状态/追踪数**无语义**；多处点击区 **< 48dp** | `UiFoundation.kt:39-68`、`:698-913`、`Screens.kt:1565-1582`、`:2123-2128`、`:2251-2260`、`TaskDetailScreen.kt:1075`、`SettingsScreen.kt:283/998` | 低视力用户 |
| ⭐⭐ 功能不可用 | **产检 / 硬 deadline 没有任何"提前提醒"**（提前量是死设置，全仓无消费点）→ 只在到点响一次，错过就没了 | `app3.js:3159-3164`、手机端无提前量字段 | 孕妇、直播主播 |
| ⭐⭐ 功能不可用 | **电脑端没有系统通知**，只有页内弹窗 → 窗口被遮挡（如直播中）就**完全看不到提醒** | `app3.js:3184`、`lib.rs` 通知插件只 init 未用 | 直播主播 |
| ⭐⭐ 数据错 | **换机 = 全丢**：无导入入口 + 买断授权码不随备份 + 等级/连续天数/徽章归零，且**没有任何"你是老用户"的识别** | `SettingsScreen.kt:958`（全仓无 import）、`License.kt`（码存 prefs） | 初三男生、全职妈妈 |
| ⭐ 数据错 | 连击（连续天数）断裂**没有补卡**，长连击反而变成负担；等级约 Lv.10 撞天花板后**成长感消失** | 接着用 × 2 | 游戏玩家、大学生 |
| ⭐ 难用 | 历史页**无搜索**、日历锚定"今天"、数据量上来后找不到东西 | 接着用 × 3 | 大学生、护士、物流司机 |

## 四、未修清单（v5.23.0 的 8 项 + 本轮新增 6 项）

**沿用**：批量恢复可刷分 ｜ 无导入 ｜ 双端覆盖与积分同步语义 ｜ 电脑端提醒与数字口径 ｜
「提醒方式/范围」死设置 ｜ 分类死代码 ｜ 挂件「+」失效与旧名 ｜ 视觉一致性一组

**本轮新增**：
1. 「每周」任务的**今日列表/日历过滤**（提醒已对，界面没对）
2. **自动升级开关复现**（UI 删了但默认开）
3. **无障碍**：LiveRegion 缺失、批量入口、< 48dp 点击区
4. **提前提醒**（产检/硬 deadline 的安全网）
5. **电脑端系统通知**（页内弹窗在窗口被遮挡时无效）
6. **睡眠/免打扰时段**（夜班作息用户的提醒会把人吵醒）

## 五、下一步建议（按性价比排序）
1. **提前提醒**（1 天/3 天/1 小时）—— 改动小、对"怕漏事"的人群价值最高
2. **每周任务的列表过滤** —— 补完 #9 的最后一半
3. **电脑端系统通知** —— 桌面端体验的硬伤
4. **导入 + 授权码随备份** —— 买断的门票（已在旧清单里，优先级应上调）
5. **自动升级开关**（要么恢复 UI、要么默认关）
6. **无障碍三件套**（LiveRegion / 批量入口 / 48dp）
7. 睡眠时段静音
